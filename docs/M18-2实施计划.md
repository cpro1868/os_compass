# M18-2: 意图搜索功能修复与向量搜索改造

> **创建时间**：2026-08-02
> **更新时间**：2026-08-03
> **状态**：✅ 方案已确认
> **核心目标**：**意图搜索功能可以正常使用**
> **前置依赖**：M17 意图搜索增强

---

## 1 问题背景与目标

### 1.1 当前问题

| # | 问题 | 影响 | 优先级 |
|---|------|------|--------|
| 1 | sqlite-vss 不支持 Windows | 向量搜索不可用 | P0 |
| 2 | 向量搜索死锁 | 搜索卡死无响应 | P0 |
| 3 | 向量存储格式问题 | 数据无法正确存储 | P1 |
| 4 | 降级策略不完善 | 向量失败时无兜底 | P2 |

### 1.2 核心目标

**M18-2 的最终目标**：意图搜索功能可以正常使用

| 目标 | 说明 | 验收标准 |
|------|------|----------|
| 向量搜索可用 | sqlite-vec 替代 sqlite-vss | 搜索返回正确结果 |
| 关键词搜索兜底 | 向量搜索失败时降级 | 仍有结果返回 |
| LLM 联网搜索 | GitHub 搜索正常 | 返回推荐项目 |
| 并行搜索 | 本地+联网同时执行 | 速度不受影响 |
| 意图分析 | LLM 意图判断 | 返回 JSON 结构 |
| 智能推荐 | ✅ 正常 | ✅ 正常 |

### 1.3 意图搜索流程

```
用户输入
    ↓
intent_search 命令 (search_cmd.rs)
    ↓
three_layer_search (search.rs)
    ├→ 本地向量搜索（embedding.rs）
    │   └→ 失败时降级 → 关键词 LIKE 搜索
    ├→ LLM 联网搜索（GitHub API）
    └→ 并行执行（tokio::join!）
    ↓
generate_smart_recommendation
    ↓
返回 SearchResult
```

---

## 2 技术方案确认

### 2.1 sqlite-vec API 验证

| 项目 | 状态 | 说明 |
|------|------|------|
| Rust crate 可用 | ✅ | `sqlite-vec = "0.1.9"` |
| Windows 编译 | ✅ | 静态链接 C 代码 |
| `sqlite3_auto_extension()` | ✅ | Rust FFI 可用 |
| vec0 虚拟表 | ✅ | 支持 primary key 列 |
| KNN 查询语法 | ✅ | `WHERE embedding MATCH ? AND k = ?` |
| Metadata 列 | ✅ | 支持 INTEGER/TEXT/FLOAT |

### 2.2 推荐方案

**建表语句**（已验证）：
```sql
CREATE VIRTUAL TABLE project_embeddings USING vec0(
    project_id INTEGER PRIMARY KEY,
    embedding FLOAT[1536]
);
```

**搜索语法**（已验证）：
```sql
SELECT project_id, distance
FROM project_embeddings
WHERE embedding MATCH '[...]'
  AND k = 10
ORDER BY distance
LIMIT 10;
```

### 2.3 依赖清单

| 依赖 | 当前状态 | 需添加 |
|------|----------|--------|
| rusqlite | ✅ 已有 | - |
| load_extension | ✅ 已有 | - |
| serde_json | ✅ 已有 | - |
| sqlite-vec | ❌ | ✅ `sqlite-vec = "0.1.9"` |

---

## 3 改造范围

### 3.1 功能模块

| # | 模块 | 当前状态 | 目标状态 |
|---|------|----------|----------|
| 1 | 向量搜索 | ❌ 不可用 | ✅ 可用 |
| 2 | 关键词搜索 | ⚠️ 降级兜底 | ✅ 正常工作 |
| 3 | LLM 联网 | ⚠️ 可能有代理问题 | ✅ 正常工作 |
| 4 | 并行搜索 | ⚠️ 可能死锁 | ✅ 正常工作 |
| 5 | 意图分析 | ⚠️ 可能失败 | ✅ 正常工作 |
| 6 | 智能推荐 | ✅ 正常 | ✅ 正常 |

### 3.2 涉及文件

| # | 文件 | 修改内容 | 风险 |
|---|------|----------|------|
| 1 | `Cargo.toml` | 添加 sqlite-vec 依赖 | 低 |
| 2 | `embedding.rs` | 向量加载 + 存储 + 搜索 | 中 |
| 3 | `db.rs` | 建表语句 | 低 |
| 4 | `search.rs` | 降级策略 + 死锁修复 | 中 |

---

## 4 详细改造内容

### 4.1 Task 1: Rust 依赖配置

**文件**：`src-tauri/Cargo.toml`

**修改**：
```toml
[dependencies]
sqlite-vec = "0.1.9"
```

### 4.2 Task 2: embedding.rs 重构

**文件**：`src-tauri/src/embedding.rs`

**修改项**：

| # | 修改项 | 当前 | 改造后 |
|---|--------|------|--------|
| 1 | 扩展初始化 | `load_extension("vss0.dll")` | `sqlite3_auto_extension()` |
| 2 | 建表语句 | 普通表 + BLOB | vec0 虚拟表 + FLOAT[1536] |
| 3 | 搜索语法 | `vss_search_params()` | `WHERE embedding MATCH ? AND k = ?` |
| 4 | 向量格式 | BLOB 二进制 | JSON 字符串 `[...]` |
| 5 | 死锁修复 | Mutex 嵌套 | RwLock 或提前检查 |

**关键代码变更**：

```rust
// 1. 扩展初始化（改造后）
use sqlite_vec::sqlite3_vec_init;

pub fn init_vec_extension() -> Result<(), String> {
    unsafe {
        rusqlite::ffi::sqlite3_auto_extension(
            Some(std::mem::transmute(sqlite3_vec_init as *const ())
        );
    }
    Ok(())
}

// 2. 向量存储（改造后）
pub fn store_embeddings(project_id: i64, embeddings: &[f32]) -> Result<(), String> {
    let embedding_json = serde_json::to_string(embeddings)
        .map_err(|e| e.to_string())?;
    
    conn.execute(
        "INSERT OR REPLACE INTO project_embeddings (project_id, embedding) VALUES (?, ?)",
        params![project_id, embedding_json],
    )?;
    Ok(())
}

// 3. 向量搜索（改造后）
pub async fn semantic_search(query: &str, limit: usize) -> Result<Vec<(i64, f32)>, String> {
    let query_json = serde_json::to_string(&query_embedding)
        .map_err(|e| e.to_string())?;
    
    let mut stmt = conn.prepare(
        "SELECT project_id, distance
         FROM project_embeddings
         WHERE embedding MATCH ?
         AND k = ?
         ORDER BY distance"
    )?;
    
    let results = stmt.query_map(params![query_json, limit as i64], |row| {
        Ok((row.get(0)?, row.get(1)?))
    })?;
    Ok(matches)
}
```

### 4.3 Task 3: db.rs 建表语句

**文件**：`src-tauri/src/db.rs`

**修改项**：

| # | 表类型 | 当前 | 改造后 |
|---|--------|------|--------|
| 1 | 类型 | 普通表 | vec0 虚拟表 |
| 2 | 列定义 | `embedding BLOB, dimension INTEGER` | `embedding FLOAT[1536]` |
| 3 | 主键 | `id INTEGER PRIMARY KEY` | `project_id INTEGER PRIMARY KEY` |

**SQL 变更**：
```sql
-- 当前（普通表）
CREATE TABLE project_embeddings (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL UNIQUE,
    embedding BLOB NOT NULL,
    dimension INTEGER NOT NULL DEFAULT 1536,
    created_at TEXT DEFAULT (datetime('now', 'localtime'))
);
CREATE INDEX idx_embeddings_project ON project_embeddings(project_id);

-- 改造后（vec0 虚拟表）
CREATE VIRTUAL TABLE project_embeddings USING vec0(
    project_id INTEGER PRIMARY KEY,
    embedding FLOAT[1536]
);
```

**优势**：
- 不需要单独的索引（虚拟表内置）
- 不需要 dimension 列（vec0 自动验证）
- 不需要 created_at 列（向量数据不变）

### 4.4 Task 4: search.rs 降级策略 + 死锁修复

**文件**：`src-tauri/src/plugins/search.rs`

**问题 1：死锁**（当前代码第 268-292 行）
```rust
// 当前（有死锁风险）
pub fn ensure_vss_loaded() -> Result<(), String> {
    let mut loaded = VSS_LOADED.lock().unwrap();  // 获取 VSS_LOADED 锁
    if *loaded {
        return Ok(());
    }
    drop(loaded);

    let db_guard = DATABASE.lock().unwrap();  // 🔒 再次获取 DATABASE 锁
    // ...
}
```

**改造方案**：使用 `RwLock` 替代嵌套 `Mutex`
```rust
// 改造后（无死锁）
use std::sync::RwLock;

static VEC_LOADED: RwLock<bool> = RwLock::new(false);

pub fn ensure_vec_loaded(conn: &rusqlite::Connection) -> Result<(), String> {
    // 检查缓存
    {
        let guard = VEC_LOADED.read().unwrap();
        if *guard {
            return Ok(());
        }
    }
    
    // 注册扩展（不需要 DATABASE 锁）
    init_vec_extension()?;
    
    // 更新缓存
    let mut guard = VEC_LOADED.write().unwrap();
    *guard = true;
    
    Ok(())
}
```

**问题 2：降级策略不完善**
```rust
// 当前（向量失败后简单降级）
if crate::embedding::is_enabled() {
    match crate::embedding::semantic_search(query, 10).await {
        Ok(matches) => { /* 使用向量结果 */ }
        Err(e) => { /* 降级但无日志 */ }
    }
}

// 改造后（完善降级 + 日志）
let local_results = if crate::embedding::is_enabled() {
    match crate::embedding::semantic_search(query, 10).await {
        Ok(matches) => {
            log::info!("向量搜索返回 {} 个结果", matches.len());
            /* 使用向量结果 */
        }
        Err(e) => {
            log::warn!("向量搜索失败，降级到关键词: {}", e);
            keyword_search(&main_conn, query)
        }
    }
} else {
    log::info!("向量搜索未启用，使用关键词搜索");
    keyword_search(&main_conn, query)
};
```

### 4.5 Task 5: 端到端测试

**测试场景**：

| # | 场景 | 预期结果 | 验证方法 |
|---|------|----------|----------|
| 1 | 新仓库首次搜索 | 向量生成 + 搜索成功 | 检查日志 |
| 2 | 向量搜索 | 返回相似项目 | 验证结果数量 |
| 3 | 向量失败降级 | 关键词搜索返回结果 | 模拟失败场景 |
| 4 | LLM 联网搜索 | 返回 GitHub 推荐 | 检查 web_results |
| 5 | 并行搜索 | 本地+联网同时返回 | 检查耗时 |
| 6 | 意图分析 | 返回正确 JSON | 验证解析 |
| 7 | 仓库切换 | 搜索状态正确重置 | 切换仓库测试 |

---

## 5 工时估算

| Task | 内容 | 预估工时 | 风险 | 优先级 |
|------|------|----------|------|--------|
| Task 1 | Cargo.toml 添加依赖 | 0.5h | 低 | P0 |
| Task 2 | embedding.rs 重构 | 2h | 中 | P0 |
| Task 3 | db.rs 建表语句 | 1h | 低 | P0 |
| Task 4 | search.rs 降级+死锁 | 1.5h | 中 | P0 |
| Task 5 | 端到端测试 | 2h | 中 | P0 |
| **总计** | | **7h** | | |

---

## 6 开发条件评估

### ✅ 已确认具备开发条件

| # | 条件 | 状态 | 依据 |
|---|------|------|------|
| 1 | sqlite-vec Rust crate | ✅ 可用 | crates.io 发布版 0.1.9 |
| 2 | Windows 编译支持 | ✅ 支持 | C 代码静态链接 |
| 3 | vec0 建表语法 | ✅ 明确 | 支持 primary key 列 |
| 4 | KNN 查询语法 | ✅ 明确 | `WHERE embedding MATCH ? AND k = ?` |
| 5 | Rust FFI API | ✅ 明确 | `sqlite3_auto_extension()` |
| 6 | 向量格式 | ✅ 明确 | JSON 字符串 |
| 7 | 死锁解决方案 | ✅ 明确 | RwLock 替代 Mutex |

### ⚠️ 需要注意的问题

| # | 问题 | 风险 | 解决方案 |
|---|------|------|----------|
| 1 | Rust crate 首次编译 | 低 | 编译时静默，1-2 分钟 |
| 2 | 数据迁移 | 中 | 保留旧表，验证后删除 |
| 3 | 向量维度验证 | 低 | vec0 自动验证 |

---

## 7 验收标准

- [ ] 向量搜索返回正确结果（相似项目）
- [ ] 向量失败时降级到关键词搜索
- [ ] LLM 联网搜索返回 GitHub 推荐
- [ ] 并行搜索不卡死
- [ ] 意图分析返回正确 JSON
- [ ] 搜索响应时间 < 5 秒
- [ ] Previous/os-compass.exe 更新

---

## 8 开发顺序

### 第一阶段：依赖 + 基础（1h）
1. 添加 sqlite-vec 依赖
2. 验证编译通过

### 第二阶段：核心改造（3.5h）
3. embedding.rs 重构
4. db.rs 建表语句

### 第三阶段：问题修复（1.5h）
5. search.rs 降级策略
6. search.rs 死锁修复

### 第四阶段：测试验证（2h）
7. 端到端功能测试
8. 构建 + 发布

---

> **最后更新**：2026-08-03
