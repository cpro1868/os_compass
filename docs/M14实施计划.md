# M14: 插件系统扩展 实施计划

> **二期功能标记**：本计划所有功能均为二期（Phase 2）功能。
> **重要概念**：区分扩展配置（获取项目信息）和功能插件（扩展系统功能）
> **本期包含**：M13 核心基础设施（Task 1-5）+ M14 功能扩展

---

## 0. 概念区分

### 扩展配置体系（项目导入）

| 组件 | 说明 | 已有 |
|------|------|------|
| SourcePlugin trait | 平台插件接口 | ✅ |
| GitHub 适配器 | 获取 GitHub 项目信息 | ✅ |
| Gitee 适配器 | 获取 Gitee 项目信息 | ✅ |
| NPM 适配器 | 获取 NPM 包信息 | ✅ |
| PyPI 适配器 | 获取 PyPI 包信息 | ✅ |
| **GitLab 适配器** | 获取 GitLab 项目信息 | 📋 Task 6 |
| **Docker Hub 适配器** | 获取 Docker 镜像信息 | 📋 Task 7 |

### 功能插件体系（系统扩展）

| 组件 | 说明 | 已有 |
|------|------|------|
| FeaturePlugin trait | 功能插件接口 | ✅ |
| Radar 插件 | 情报雷达 | ✅ |
| Search 插件 | 意图搜索 | ✅ |
| **插件热加载** | 加载第三方插件 | 📋 Task 8 |
| **本地插件管理** | 管理本地插件 | 📋 Task 9 |

---

## 1 概述

### 1.1 目标

本期交付完整能力：
1. **M13 核心基础设施**：FeaturePlugin + PluginManager + SourceAdapter + 4种适配器
2. **扩展配置**：GitLab/Docker Hub 平台支持（项目导入）
3. **功能插件**：插件热加载 + 本地插件管理（系统扩展）

### 1.2 架构

```
┌─────────────────────────────────────────────────────────────┐
│                     应用层                                   │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐         │
│  │ 插件市场 UI  │  │ 快捷键系统   │  │ 新手引导    │         │
│  └─────────────┘  └─────────────┘  └─────────────┘         │
└─────────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────────┐
│                    插件管理层                                │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ PluginManager（增强）                                │   │
│  │ - 内置插件：radar/search                            │   │
│  │ - 热加载插件：load_from_dir()                       │   │
│  │ - 插件市场：fetch_remote_plugins()                 │   │
│  └─────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
                           │
┌─────────────────────────────────────────────────────────────┐
│                    插件域                                    │
│  ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐       │
│  │ GitLab  │  │DockerHub│  │ 第三方   │  │ 预留    │       │
│  │ Adapter │  │ Adapter │  │  插件    │  │ 插槽    │       │
│  └─────────┘  └─────────┘  └─────────┘  └─────────┘       │
└─────────────────────────────────────────────────────────────┘
```

### 1.3 技术栈

- Rust + Tauri 2.0 + React 18 + TypeScript 5
- SQLite + Tokio + serde + tokio::fs
- GitLab API (reqwest) + Docker Hub API

---

## 2 功能详情

### 2.1 扩展配置：GitLab 适配器

**功能**：支持从 GitLab 导入项目。

> **使用场景**：项目导入（用户输入 GitLab URL 导入新项目）

| 功能点 | 说明 |
|--------|------|
| GitLab URL 解析 | 识别 gitlab.com 和私有 GitLab 实例 |
| API Token 配置 | 支持 Personal Access Token（通过扩展配置） |
| 项目导入 | API 获取项目信息（名称、描述、星标、语言、提交活跃度） |
| 健康度评分 | GitLab 特有维度：CI/CD 配置、MR 合并率、issue 关闭率 |

**注意**：GitLab 适配器实现 `SourcePlugin` 接口，与 GitHub/Gitee 适配器同属扩展配置体系。

### 2.2 扩展配置：Docker Hub 适配器

**功能**：支持从 Docker Hub 导入镜像信息。

> **使用场景**：项目导入（用户输入 Docker 镜像名导入信息）

| 功能点 | 说明 |
|--------|------|
| 镜像解析 | 解析 official/namespace/image 格式 |
| 官方 API | https://hub.docker.com/v2/repositories/{name} |
| 数据获取 | 下载量、拉取趋势、星标、标签列表 |
| 项目卡片 | Docker 特有字段：基础镜像、Dockerfile、容器化程度 |

**注意**：Docker Hub 适配器实现 `SourcePlugin` 接口。

### 2.3 功能插件：插件热加载

**功能**：支持加载本地目录中的第三方功能插件。

> **技术方案**：先实现目录加载（简单），WASM 支持后续实现。

| 功能点 | 说明 |
|--------|------|
| 插件目录 | 扫描 `${AppData}/.os-compass/plugins/` 目录 |
| 插件清单 | `manifest.json` 描述插件元数据 |
| 动态注册 | 运行时注册到 PluginManager |
| 生命周期 | init/start/stop/destroy |
| 隔离沙箱 | 插件错误不影响主程序 |

**注意**：这是 FeaturePlugin 的热加载，与 SourcePlugin 是两个独立体系。

### 2.4 功能插件：本地插件管理

**功能**：管理本地已安装的功能插件。

> **注意**：本期不包含远程插件市场，仅实现本地插件管理。

| 功能点 | 说明 |
|--------|------|
| 插件列表 | 显示 `${AppData}/.os-compass/plugins/` 下的插件 |
| 插件详情 | 版本、作者、描述、权限 |
| 启用/禁用 | 开关控制插件是否加载 |
| 卸载插件 | 删除插件目录 |

**注意**：radar/search 作为内置插件，始终存在且可启用/禁用。

### 2.5 备份恢复

**功能**：仓库文件夹级别的备份与恢复。

| 功能点 | 说明 |
|--------|------|
| 作用域 | 单个仓库，只备份当前仓库目录下的所有文件 |
| 备份粒度 | 整个仓库文件夹打包为 .zip |
| 手动备份 | 用户输入文件名，保存到用户选择的位置 |
| 自动备份 | 按表达式自动命名，保存到用户选择的位置 |
| 同名处理 | 覆盖 |
| 恢复目标 | 用户选择目录（新仓库） |
| 目录冲突 | 提示"目录不为空"，用户确认后覆盖 |
| 恢复验证 | 数据库文件存在、密钥文件存在、版本兼容 |

**注意**：备份恢复是文件级别操作，直接操作 .zip 文件，**不需要数据库表**记录备份历史。备份列表直接从用户选择的目录读取 .zip 文件。

### 2.6 新手引导

**功能**：首次使用引导流程。

| 功能点 | 说明 |
|--------|------|
| 欢迎页 | 介绍产品功能 |
| 仓库创建 | 创建第一个仓库 |
| LLM 配置 | 配置 AI 能力 |
| 第一个项目 | 导入第一个开源项目 |
| 引导完成 | 跳转到主界面 |

### 2.7 快捷键

**功能**：全局快捷键支持。

| 快捷键 | 功能 |
|--------|------|
| `Ctrl+K` | 打开命令面板/搜索 |
| `Ctrl+N` | 新建项目导入 |
| `Ctrl+,` | 打开设置 |
| `Ctrl+/` | 打开快捷键帮助 |
| `Escape` | 关闭弹窗 |

---

## 3 任务分解

### 3.1 M13 核心基础设施（本期开发）

| Task | 内容 | 工时 | 说明 |
|------|------|------|------|
| **Task 1** | FeaturePlugin trait + PluginManager | 1d | 插件基础设施 |
| **Task 2** | PluginConfig DB（plugins.db） | 0.5d | 插件配置库 |
| **Task 3** | 插件命令（list/enable/config） | 0.5d | 5 个 Tauri 命令 |
| **Task 4** | SourceAdapter trait + 4种适配器 | 2d | RSS/WebCrawl/Telegram/PlatformPreset |
| **Task 5** | LLM 解析器 | 0.5d | 原始内容 -> 项目信息 |

**M13 核心基础设施小计**：4.5d

### 3.2 M14 功能扩展

| Task | 内容 | 依赖 | 工时 |
|------|------|------|------|
| **Task 6** | GitLab 适配器（SourcePlugin） | Task 4 | 1d |
| **Task 7** | Docker Hub 适配器（SourcePlugin） | Task 4 | 1d |
| **Task 8** | 插件热加载（FeaturePlugin） | Task 1 | 1.5d |
| **Task 9** | 本地插件管理 UI | Task 8 | 1d |
| **Task 10** | 备份恢复功能 | - | 0.5d |
| **Task 11** | 新手引导 | - | 0.5d |
| **Task 12** | 快捷键系统 | - | 0.5d |
| **Task 13** | i18n + 构建验证 | Task 1-12 | 0.3d |

**M14 功能扩展小计**：6.3d

### 3.3 总计

| 阶段 | 任务数 | 工时 |
|------|--------|------|
| M13 核心基础设施 | 5 | 4.5d |
| M14 功能扩展 | 8 | 6.3d |
| **总计** | **13** | **10.8d** |

---

## 4 实施步骤

### Task 1: FeaturePlugin trait + PluginManager

**Files:**
- New: `src-tauri/src/feature_plugin.rs`
- New: `src-tauri/src/plugin_manager.rs`
- Modify: `src-tauri/src/lib.rs`

**Step 1: 创建 feature_plugin.rs**

```rust
pub trait FeaturePlugin: Send + Sync {
    fn plugin_id(&self) -> &str;
    fn plugin_name(&self) -> &str;
    fn plugin_type(&self) -> FeaturePluginType;
    fn is_enabled(&self) -> bool;
    async fn init(&self, context: &PluginContext) -> Result<(), PluginError>;
}

pub enum FeaturePluginType {
    Radar,
    Search,
    Custom,
}
```

**Step 2: 创建 plugin_manager.rs**

```rust
pub struct PluginManager {
    registry: PluginRegistry,
    plugins: HashMap<String, Box<dyn FeaturePlugin>>,
}

impl PluginManager {
    pub fn new() -> Self;
    pub fn register(&mut self, plugin: Box<dyn FeaturePlugin>);
    pub fn enable(&mut self, plugin_id: &str) -> Result<(), PluginError>;
    pub fn disable(&mut self, plugin_id: &str) -> Result<(), PluginError>;
}
```

**Step 3: 编译 + 提交**

---

### Task 2: PluginConfig DB（plugins.db）

**Files:**
- New: `src-tauri/src/plugin_config_db.rs`
- Modify: `src-tauri/src/lib.rs`

**Step 1: 创建插件配置库**

- 位置：`${AppData}/.os-compass/plugins.db`
- 表：system_plugins（id/name/plugin_type/version/enabled/config）
- 预置数据：radar/search

**Step 2: 编译 + 提交**

---

### Task 3: 插件命令

**Files:**
- New: `src-tauri/src/commands/plugin_cmd.rs`
- Modify: `src-tauri/src/commands/mod.rs`

**Step 1: 实现 5 个命令**

```rust
#[tauri::command]
fn list_feature_plugins() -> Result<Vec<PluginInfo>, String>;

#[tauri::command]
fn set_plugin_enabled(plugin_id: &str, enabled: bool) -> Result<(), String>;

#[tauri::command]
fn get_plugin_config(plugin_id: &str) -> Result<String, String>;

#[tauri::command]
fn save_plugin_config(plugin_id: &str, config: &str) -> Result<(), String>;

#[tauri::command]
fn plugin_get_db_path(plugin_id: &str) -> Result<Option<String>, String>;
```

**Step 2: 编译 + 提交**

---

### Task 4: SourceAdapter trait + 4种适配器

**Files:**
- New: `src-tauri/src/source_engine/mod.rs`
- New: `src-tauri/src/source_engine/rss_adapter.rs`
- New: `src-tauri/src/source_engine/webcrawl_adapter.rs`
- New: `src-tauri/src/source_engine/telegram_adapter.rs`
- New: `src-tauri/src/source_engine/platform_preset.rs`
- Modify: `src-tauri/src/lib.rs`

**Step 1: 创建 source_engine/mod.rs**

```rust
pub trait SourceAdapter: Send + Sync {
    fn adapter_type(&self) -> &str;
    async fn fetch(&self, url: &str, proxy: Option<&str>) -> Result<Vec<RawContent>, SourceError>;
}

pub struct RawContent {
    pub title: String,
    pub url: String,
    pub content: String,
    pub published_at: Option<String>,
}

pub fn get_adapter(source_type: &str) -> Option<Box<dyn SourceAdapter>>;
```

**Step 2: 创建 4 种适配器**

- RssAdapter：解析 RSS/Atom
- WebCrawlAdapter：调用 web2md 爬虫
- TelegramAdapter：提取 TG 频道链接
- PlatformPresetAdapter：GitHub/Gitee Trending

**Step 3: 编译 + 提交**

---

### Task 5: LLM 解析器

**Files:**
- New: `src-tauri/src/source_engine/llm_parser.rs`

**Step 1: 实现 LLM 解析**

```rust
pub struct ParsedProjectInfo {
    pub project_name: String,
    pub project_url: String,
    pub description: String,
    pub language: Option<String>,
    pub keywords: Vec<String>,
}

pub async fn parse_content_with_llm(
    raw_content: &RawContent,
    settings: &AppSettings,
) -> Result<Vec<ParsedProjectInfo>, String>;
```

**Step 2: 编译 + 提交**

---

### Task 6: GitLab 适配器（扩展配置）

**Files:**
- New: `src-tauri/src/source_engine/gitlab_adapter.rs`
- Modify: `src-tauri/src/source_engine/mod.rs`

**Step 1: 实现 GitLabSourcePlugin**

```rust
pub struct GitLabSourcePlugin;

impl SourcePlugin for GitLabSourcePlugin {
    fn match_url(&self, url: &str) -> bool {
        url.contains("gitlab.com") || url.contains("/gitlab/")
    }

    async fn fetch_metadata(&self, url: &str) -> Result<ProjectMetadata, PluginError>;
}
```

**Step 2: 健康度评分**

- CI/CD 配置
- MR 合并率
- Issue 闭环率
- 提交活跃度

**Step 3: 编译 + 提交**

---

### Task 7: Docker Hub 适配器（扩展配置）

**Files:**
- New: `src-tauri/src/source_engine/dockerhub_adapter.rs`
- Modify: `src-tauri/src/source_engine/mod.rs`

**Step 1: 实现 DockerHubSourcePlugin**

```rust
pub struct DockerHubSourcePlugin;

impl SourcePlugin for DockerHubSourcePlugin {
    fn match_url(&self, url: &str) -> bool {
        url.contains("docker.io") || url.contains("hub.docker.com")
    }

    async fn fetch_metadata(&self, url: &str) -> Result<ProjectMetadata, PluginError>;
}
```

**Step 2: 编译 + 提交**

---

### Task 8: 插件热加载（功能插件）

**Files:**
- New: `src-tauri/src/plugin_loader.rs`
- Modify: `src-tauri/src/plugin_manager.rs`

**Step 1: 扫描插件目录**

扫描 `${AppData}/.os-compass/plugins/` 目录。

**Step 2: 加载 manifest.json**

```rust
pub struct PluginManifest {
    pub id: String,
    pub name: String,
    pub version: String,
    pub author: String,
    pub description: String,
    pub entry: String,
    pub permissions: Vec<String>,
}
```

**Step 3: 注册到 PluginManager**

**Step 4: 编译 + 提交**

---

### Task 9: 本地插件管理 UI

**Files:**
- New: `src/components/LocalPluginManager.tsx`
- Modify: `src/components/SettingsDialog.tsx`

**Step 1: 插件列表页面**

显示内置插件 + 本地插件。

**Step 2: 启用/禁用功能**

调用 setPluginEnabled。

**Step 3: 编译 + 提交**

---

### Task 10: 备份恢复功能

**Files:**
- New: `src-tauri/src/commands/backup_cmd.rs`

**Step 1: 实现备份**

- 打包当前仓库目录为 .zip
- 用户输入文件名，保存到选择的位置

**Step 2: 实现恢复**

- 解压 .zip 到目标目录
- 验证数据库、密钥、版本

**Step 3: 编译 + 提交**

---

### Task 11: 新手引导

**Files:**
- New: `src/components/OnboardingGuide.tsx`
- Modify: `src/App.tsx`

**Step 1: 5 步引导流程**

- 欢迎
- 创建仓库
- 配置 LLM
- 导入项目
- 完成

**Step 2: 使用 localStorage 记录状态**

**Step 3: 编译 + 提交**

---

### Task 12: 快捷键系统

**Files:**
- New: `src/hooks/useKeyboardShortcuts.ts`
- Modify: `src/App.tsx`

**Step 1: 快捷键 Hook**

```typescript
export const useKeyboardShortcuts = () => {
  // Ctrl+K: 命令面板
  // Ctrl+N: 新建项目
  // Ctrl+,: 设置
  // Ctrl+/: 快捷键帮助
};
```

**Step 2: 存储在 app_settings.settings JSON 中**

**Step 3: 编译 + 提交**

---

### Task 13: i18n + 构建验证

**Files:**
- Modify: `src/locales/zh.json`
- Modify: `src/locales/en.json`

**Step 1: 添加翻译键**

- pluginMarket
- backup
- onboarding
- shortcuts

**Step 2: 构建验证**

```bash
$env:PATH="D:\Soft\msys64\ucrt64\bin;D:\Soft\msys64\usr\bin;$env:PATH"
cd os-compass
pnpm tauri build
```

**Step 3: 复制到 Previous + 提交**

---

## 5 Self-Review Checklist

- [ ] GitLab 适配器能导入 GitLab 项目（名称、描述、星标）
- [ ] Docker Hub 适配器能获取镜像统计（下载量、标签）
- [ ] 插件热加载能扫描目录并注册插件
- [ ] 本地插件管理 UI 显示内置/本地插件
- [ ] 备份功能能创建、列出、恢复备份
- [ ] 新手引导流程完整（5 步）
- [ ] 快捷键正常响应（Ctrl+K/Ctrl+N/Ctrl+,）
- [ ] i18n 键一致
- [ ] release 构建成功

---

## 6 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| GitLab API 变更 | 功能失效 | 版本锁定 + 错误处理 |
| 插件安全 | 恶意插件 | 权限沙箱 + 签名验证（未来） |
| Docker Hub API 限流 | 数据获取失败 | 添加重试 + 缓存 |

---

**文档结束**
