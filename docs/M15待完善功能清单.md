# M15 待完善功能清单

**日期**：2026-07-23
**状态**：M15 开发完成，待补全

---

## 一、未完成任务

### Task 3-4：情报雷达搜索增强 ❌ 未完成

**重要性**：⭐⭐⭐⭐⭐（核心功能）

| 子任务 | 内容 | 工时 |
|--------|------|------|
| Task 3 | 后端 API：关键字搜索、来源筛选、日期筛选、分页 | 0.4d |
| Task 4 | 前端 UI：搜索工具栏、分页组件 | 0.5d |

### Task 8：词库文件管理 ❌ 未完成

**重要性**：⭐⭐⭐⭐

| 子任务 | 内容 |
|--------|------|
| 8.1 | 将 zh_spam_lexicon.json 移到系统库目录 |
| 8.2 | 首次启动自动创建默认词库（如果不存在） |
| 8.3 | 在广告设置 UI 显示词库路径 |
| 8.4 | 添加"打开文件夹"按钮定位到词库文件 |
| 8.5 | 词库修改后支持重新加载 |

### Task 15：路径管理整改 ✅ 已完成

| 子任务 | 状态 |
|--------|------|
| 15.1 | ✅ 创建 system_config 表 |
| 15.2 | ✅ 创建 app_paths.rs 模块 |
| 15.3 | ✅ 替换 vault.rs 硬编码 |
| 15.4 | ✅ 替换 lib.rs 路径获取 |

---

## 二、已完成修复

### Bug 1：雷达记录删除无确认 ✅ 已修复

| 项目 | 内容 |
|------|------|
| 修改 | `handleDeleteItem` 改为设置 `deleteConfirmId`，新增 `confirmDeleteItem` 函数和确认对话框 |

### Bug 2：雷达记录删除提示英文 ✅ 已修复

| 项目 | 内容 |
|------|------|
| 新增 i18n 键 | `radar.deletedSuccess`、`radar.deleteConfirmTitle`、`radar.deleteConfirmMsg` |

### Bug 3：仓库路径指向 Roaming ✅ 已修复（2026-07-23）

| 项目 | 内容 |
|------|------|
| 问题 | `app_local_data_dir()` 在 Tauri 2.0 返回 Roaming |
| 修复 | 改用 `directories::BaseDirs::data_local_dir()` |
| 修改文件 | `vault.rs`、`lib.rs`、`vault_cmd.rs` |

---

## 三、待检查项

### 检查 1：其他模块删除功能

| 模块 | 文件 | 是否有确认 |
|------|------|-----------|
| 列表视图 | `ListView.tsx` | 待检查 |
| 看板视图 | `KanbanView.tsx` | 待检查 |
| 归档视图 | `ArchiveView.tsx` | 待检查 |
| 雷达信息源 | `RadarInbox.tsx` | ✅ 已确认（有确认） |

---

### Task 16：AI 分析问题 ❌ 未完成

**重要性**：⭐⭐⭐⭐

**问题**：点击重新分析后长时间无输出（超过 30 秒）

**排查方向**：
1. 检查 `analyze_project` 函数超时设置
2. 添加进度日志输出
3. 前端添加加载状态提示
4. 检查网络请求是否正常发送

---

## 四、任务统计

| 类别 | 数量 |
|------|------|
| 未完成功能 | 4（Task 3-4、Task 8、Task 16） |
| 已完成 | 1（Task 15） |
| 已修复 Bug | 3 |
| 待检查项 | 1 |

---

**最后更新**：2026-07-23
**负责人**：待定
