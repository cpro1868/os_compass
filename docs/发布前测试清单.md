# OS-Compass 发布前测试清单

> **重要**：每次发布（更新 Previous 目录）前，必须完成本清单所有检查项。
> **原则**：发布的程序必须能够使用，不能将测试丢给用户。

---

## P0 - 必须完成（阻塞发布）

### 1. 数据库初始化验证

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **清理旧数据库** | 删除 `os_compass.db`、`system_settings.db` 等数据库文件 | 数据库文件已删除 | ⬜ |
| **首次启动** | 启动程序 | 程序正常启动，无崩溃 | ⬜ |
| **扩展配置页面** | 打开设置 → 扩展配置 | 显示 GitHub、Gitee、Crawler 三个插件 | ⬜ |
| **系统变量页面** | 打开设置 → 系统变量 | 显示 github_token、gitee_token、github_proxy | ⬜ |
| **url_patterns 字段** | 检查 source_plugins 表 | github 的 url_patterns 包含 "github.com" | ⬜ |

### 2. 配置持久化验证

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **LLM 配置保存** | 设置 → LLM 配置 → 填写 API Key → 保存 → 重启 | 重启后 API Key 仍存在 | ⬜ |
| **主题保存** | 设置 → 主题 → 切换为 light → 保存 → 重启 | 重启后主题仍为 light | ⬜ |
| **代理配置保存** | 设置 → 代理 → 填写代理地址 → 保存 → 重启 | 重启后代理配置仍存在 | ⬜ |

### 3. 核心功能验证

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **项目导入** | 导入 GitHub 项目 | 项目成功导入，显示基本信息 | ⬜ |
| **扩展配置** | 配置 GitHub Token → 导入项目 | 使用 Token 方式获取数据 | ⬜ |
| **项目列表** | 查看导入的项目列表 | 项目正确显示在列表中 | ⬜ |

---

## P1 - 强烈建议（影响体验）

### 4. 国际化验证

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **中文界面** | 默认语言为中文 | 所有界面显示中文 | ⬜ |
| **英文界面** | 切换为英文 | 所有界面显示英文 | ⬜ |

### 5. 暗色模式验证

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **暗色主题** | 主题设为 dark | 所有组件正确显示暗色样式 | ⬜ |
| **亮色主题** | 主题设为 light | 所有组件正确显示亮色样式 | ⬜ |

---

## P2 - 建议测试（非阻塞）

### 6. 情报雷达功能验证（如已实现）

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **雷达视图** | 切换到雷达视图 | 视图正常显示 | ⬜ |
| **扫描功能** | 点击立即扫描 | 扫描任务执行，进度显示 | ⬜ |

### 7. 意图搜索功能验证（如已实现）

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **搜索视图** | 切换到搜索视图 | 视图正常显示 | ⬜ |
| **搜索功能** | 输入关键词搜索 | 返回搜索结果 | ⬜ |

---

## 代码与脚本一致性检查

| 检查项 | 操作 | 预期结果 | 状态 |
|--------|------|---------|------|
| **db.rs 与 init_schema.sql 一致性** | 对比两个文件中的表结构 | 所有表结构完全一致 | ⬜ |
| **种子数据一致性** | 对比两个文件中的 INSERT 语句 | 所有种子数据完全一致 | ⬜ |

---

## 发布前检查流程

```bash
# 1. 清理旧数据库
rm -f "$LOCALAPPDATA/com.kimicode.os-compass/os_compass.db"
rm -f "$LOCALAPPDATA/com.kimicode.os-compass/system_settings.db"
rm -f "$LOCALAPPDATA/com.kimicode.os-compass/system_vars.db"

# 2. 构建
$env:PATH="D:\Soft\msys64\ucrt64\bin;D:\Soft\msys64\usr\bin;$env:PATH"
cd "G:\Projects\kimicode\os_compass\os-compass"
pnpm tauri build

# 3. 手动测试（按上表执行）

# 4. 更新 Previous 目录
Copy-Item "src-tauri\target\release\os-compass.exe" "Previous\os-compass.exe"
Copy-Item "src-tauri\target\release\WebView2Loader.dll" "Previous\WebView2Loader.dll"
Copy-Item "scripts\init_schema.sql" "Previous\scripts\init_schema.sql"

# 5. Git 提交
git add .
git commit -m "release: v2.x.x - [本次发布的主要功能或修复]"
```

---

## 事件记录

| 日期 | 事件 | 教训 |
|------|------|------|
| 2026-07-14 | 扩展配置丢失 Bug | 必须验证数据库初始化脚本与代码一致 |

---

**最后更新**: 2026-07-14
