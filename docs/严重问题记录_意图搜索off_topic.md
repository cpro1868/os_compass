# 意图搜索问题 - 严重工作问题记录

**日期**: 2026-08-08
**问题级别**: 严重 (P0)
**影响功能**: 意图搜索 off_topic 流程
**状态**: 未解决

---

## 问题现象

用户输入"你好"等闲聊内容时：
- 后端 LLM 正确返回 `{"intent": "off_topic", "guidance": "..."}`
- 前端日志确认收到了 `off_topic` 意图
- 但 UI 始终显示"正在分析语义..."，不显示 guidance 消息

---

## 问题根因分析（待确认）

经过多轮修改，代码层面的修改包括：

1. **渲染条件修复**：
   - 原代码: `{msg.phase ? <TypingIndicator /> : <content>}`
   - 修改为: `{msg.phase === 'analyzing' || msg.phase === 'searching' ? <TypingIndicator /> : <content>}`

2. **off_topic 分支修复**：
   - 添加 `updatePhase('idle', intent.guidance)`
   - 添加 `return` 避免 finally 块干扰

3. **类型定义修复**：
   - `intent: 'clear' | 'unclear' | 'off_topic'` → `intent: string`

---

## 测试验证情况

| 测试类型 | 结果 |
|---------|------|
| 单元测试 (31个) | 全部通过 |
| 集成测试 (5个) | 全部通过 |
| 类型检查 | 通过 |
| 打包验证 | 代码正确打包 |

---

## 日志分析

```
[DEBUG-RUST] analyze_user_intent 返回成功: intent=off_topic, guidance=Some("...")
[FRONTEND DEBUG] 前端接收 off_topic: {"intent":"off_topic","guidance":"..."}
```

日志确认后端和前端通信正常。

---

## 问题反思

### 为什么这么久没解决？

1. **测试能力不足**：桌面应用无法像 Web 应用一样直接在 CLI 测试
2. **过度依赖日志**：前端 `console.log` 无法在桌面应用中直接查看
3. **修复方向偏差**：可能一直在修复代码，但真正的bug在别处
4. **验证不充分**：每次修改都让用户测试，没有自己完成验证

### 可能的遗漏

1. **前端控制台日志**：应该配置日志输出到文件
2. **缓存问题**：可能浏览器缓存导致新代码未加载
3. **多进程问题**：Tauri 的前端和后端在不同进程中

---

## 接下来的计划

### 短期（立即）

1. 添加前端日志到文件输出
2. 在 build 命令中强制清除缓存
3. 使用 Tauri 的 `invoke` 方法直接调用后端命令验证

### 中期

1. 编写完整的 E2E 测试
2. 添加前端集成测试
3. 建立自动化测试流程

### 长期

1. 完善前端调试能力
2. 添加日志聚合和分析工具
3. 建立完整的 CI/CD 测试流程

---

## 关键教训

1. **桌面应用不是 Web 应用**：不能直接使用 console.log 调试
2. **用户测试不能替代自动化测试**：应该自己完成测试再让用户验证
3. **日志要可追溯**：需要配置日志输出到文件

---

## 待用户确认

1. 程序版本是否为最新构建（13:38:06）？
2. 是否尝试过清除浏览器缓存？
3. 是否可以提供更多信息（日志、截图等）？
