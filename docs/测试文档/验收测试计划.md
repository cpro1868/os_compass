# OS-Compass 验收测试计划

> **版本**：V1.0
> **日期**：2026-06-29
> **适用阶段**：Phase 0 完成后、Phase 1-1 MVP 发版前
> **目标读者**：开发团队、测试团队、项目负责人

---

## 1. 测试目标

本文档用于验证 OS-Compass 是否满足 MVP 核心功能目标：

1. **项目导入**：GitHub/Gitee/NPM/PyPI 平台 URL 自动识别和导入
2. **AI 分析**：健康度评分、AI 报告生成、README 翻译
3. **本地联动**：git clone 下载、IDE 打开
4. **数据安全**：敏感信息加密存储

## 2. 测试范围

### 2.1 覆盖范围

| 模块 | 测试内容 |
|------|----------|
| 项目导入 | URL 解析、平台识别、GitHub/Gitee/NPM/PyPI 插件 |
| 项目列表 | 看板视图、列表视图、拖拽排序、筛选 |
| 项目详情 | 概览、AI 报告、Runbook、README、笔记 |
| 分类管理 | 分类 CRUD、多层级树形结构 |
| 标签系统 | 标签 CRUD、LLM 提取、筛选 |
| AI 能力 | LLM 配置、健康度评分、翻译 |
| 本地联动 | git clone、IDE 打开 |
| 设置中心 | LLM 设置、代理设置 |
| 数据安全 | 加密存储、数据保险箱 |

### 2.2 暂不覆盖

| 模块 | 说明 |
|------|------|
| 国际化 | Phase 1-2 功能（已覆盖） |
| 批量操作 | Phase 1-2 功能 |
| 归档功能 | Phase 1-2 功能 |
| 仓库管理 | Phase 1-2 功能（已覆盖） |

### 2.3 M13 二期测试范围

| 模块 | 测试内容 |
|------|----------|
| 插件基础设施 | FeaturePlugin trait、DbMode、PluginManager 启停 |
| 信息源引擎 | RSS/WebCrawl/Telegram/PlatformPreset 适配器 |
| LLM 解析器 | 原始内容解析为项目信息 |
| 情报雷达 | 定时扫描、URL 去重、收件箱操作、通知推送 |
| 意图搜索 | 三层搜索、搜索历史、联网策略 |
| 前端集成 | RadarInbox、SearchView、插件管理面板 |
| 仓库隔离 | vault 模式数据库跟随仓库切换 |

### 2.4 M13 暂不覆盖

| 模块 | 说明 |
|------|------|
| 插件热加载 | M14 功能 |
| 第三方插件 | M14 功能 |
| 插件市场 UI | M14 功能 |

## 3. 测试环境

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10+ / macOS / Linux |
| Tauri | 2.0+ |
| Node.js | 18+ |
| pnpm | 8+ |
| Rust | 1.75+ |
| Git | 2.30+ |
| WebView2 | Windows 10 自带 |
| VS Code（可选） | 最新稳定版 |

### 3.1 测试账号

| 账号类型 | 要求 |
|----------|------|
| GitHub Token | 可选，用于提升 API 限额 |
| Gitee Token | 推荐，Gitee API 必须 |
| LLM API Key | 必须，OpenAI/DeepSeek/Ollama |
| 代理 | 可选，访问 GitHub 需要 |

## 4. 核心测试用例（P0）

### 4.1 项目导入测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P0-001** | GitHub 公开仓库导入 | `https://github.com/facebook/react` | 成功导入，显示元数据 |
| **P0-002** | GitHub 私有仓库导入 | GitHub Token + 私有仓库 URL | 成功导入（需 Token） |
| **P0-003** | Gitee 仓库导入 | Gitee Token + Gitee URL | 成功导入（需 Token） |
| **P0-004** | NPM 包导入 | `https://npmjs.com/package/react` | 成功导入，显示下载量等 |
| **P0-005** | PyPI 包导入 | `https://pypi.org/project/django` | 成功导入，显示版本信息 |
| **P0-006** | 未知平台回退 | 任意未知 URL | 提示爬虫失败或手动录入 |
| **P0-007** | 重复导入检测 | 已存在的 URL | 提示项目已存在 |

### 4.2 项目列表测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P0-008** | 看板视图显示 | 导入项目后查看 | 四列状态正常显示 |
| **P0-009** | 看板拖拽 | 拖动卡片到其他列 | 状态更新成功 |
| **P0-010** | 列表视图筛选 | 按分类/语言/标签筛选 | 筛选结果正确 |
| **P0-011** | 关键字搜索 | 搜索项目名称 | 模糊匹配结果 |

### 4.3 项目详情测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P0-012** | 查看概览 | 点击项目卡片 | 显示基本信息、AI 总结 |
| **P0-013** | 查看 AI 报告 | 切换到 AI 报告标签 | 显示健康度评分 |
| **P0-014** | 查看 README | 切换到 README 标签 | Markdown 正常渲染 |
| **P0-015** | 本地笔记 | 添加笔记 | 保存成功 |

### 4.4 AI 能力测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P0-016** | LLM 配置 | 配置 API Key 并测试连接 | 连接成功提示 |
| **P0-017** | AI 分析 | 导入时勾选 AI 分析 | 生成健康度评分 |
| **P0-018** | README 翻译 | 切换到非原文语言 | 显示翻译版本 |

### 4.5 本地联动测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P0-019** | git clone | 点击下载到本地 | 下载成功，状态更新 |
| **P0-020** | VS Code 打开 | 点击用 VS Code 打开 | VS Code 打开目录 |

### 4.6 数据安全测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P0-021** | 敏感信息加密 | 添加 Token 等敏感信息 | 加密存储成功 |
| **P0-022** | 敏感信息解密 | 查看敏感信息 | 解密显示正常 |

## 5. 扩展测试用例（P1）

### 5.1 分类管理测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P1-001** | 创建分类 | 填写名称/描述 | 分类创建成功 |
| **P1-002** | 创建子分类 | 指定父分类 | 树形结构正确 |
| **P1-003** | 删除空分类 | 分类下无项目 | 删除成功 |
| **P1-004** | 删除有项目分类 | 分类下有项目 | 提示禁止删除 |
| **P1-005** | AI 自动归类 | 导入时 AI 归类 | 分类 ID 正确 |

### 5.2 标签系统测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P1-006** | LLM 提取标签 | 导入时 LLM 提取 | 标签生成成功 |
| **P1-007** | 用户添加标签 | 手动添加标签 | 标签添加成功 |
| **P1-008** | 标签筛选 | 按标签筛选 | 筛选结果正确 |

### 5.3 网络与容错测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P1-009** | 网络超时 | 模拟网络超时 | 显示超时提示 |
| **P1-010** | LLM 调用失败 | 模拟 LLM 错误 | 显示错误提示 |
| **P1-011** | 离线浏览 | 断网状态 | 离线浏览已导入项目 |

### 5.4 代理设置测试

| 编号 | 场景 | 输入 | 预期结果 |
|------|------|------|----------|
| **P1-012** | HTTP 代理 | 配置 HTTP 代理 | GitHub 访问正常 |
| **P1-013** | SOCKS5 代理 | 配置 SOCKS5 代理 | GitHub 访问正常 |
| **P1-014** | 代理认证 | 配置用户名密码 | 代理认证成功 |

## 6. API 契约测试

### 6.1 项目导入 API

```
POST /api/projects/import
{
  "url": "https://github.com/facebook/react",
  "auto_analyze": true,
  "auto_translate": true
}
```

**成功响应**：
```json
{
  "status": "success",
  "data": {
    "id": 1,
    "name": "react",
    "lifecycle_status": "TO_EXPLORE",
    "data_status": "ACTIVE",
    "health_score": 85
  }
}
```

**失败响应**：
```json
{
  "status": "error",
  "error": {
    "code": "INVALID_URL",
    "message": "URL 格式不正确"
  }
}
```

### 6.2 错误码基线

| 错误码 | 含义 |
|--------|------|
| `INVALID_URL` | URL 格式无效 |
| `PLUGIN_NOT_FOUND` | 不支持的平台 |
| `NETWORK_FAILED` | 网络请求失败 |
| `DUPLICATE_PROJECT` | 项目已存在 |
| `LLM_NOT_CONFIGURED` | 未配置 LLM |
| `LLM_API_ERROR` | LLM 调用失败 |
| `SYSTEM_CATEGORY` | 系统分类不可删除 |
| `HAS_CHILDREN` | 存在子分类 |

## 7. M13 API 契约测试

### 7.1 插件管理 API

```
list_feature_plugins() -> Vec<PluginInfo>
set_plugin_enabled(plugin_id: String, enabled: bool) -> void
get_plugin_config(plugin_id: String) -> Option<String>
save_plugin_config(plugin_id: String, config: String) -> void
plugin_get_db_path_cmd(plugin_id: String) -> String
```

### 7.1.1 插件配置存储架构（新增）

| 存储层级 | 位置 | 说明 |
|---------|------|------|
| 插件域 | `${AppData}/.os-compass/plugins.db` | 系统级插件配置（enabled、config） |
| 仓库域 | 当前仓库 `os_compass.db` | LLM/代理配置（运行时注入） |
| 业务域 | `{vault_dir}/plugin_*.db` | 插件业务数据 |

### 7.2 雷达 API

```
list_radar_sources() -> Vec<RadarSource>
add_radar_source(name, source_type, url, platform?, check_interval?) -> RadarSource
update_radar_source(id, name?, url?, enabled?, check_interval?) -> void
delete_radar_source(id) -> void
get_radar_items(status?, limit?) -> Vec<RadarItem>
radar_item_action(item_id, action, category_id?) -> {projectId?}
trigger_radar_scan(source_id?) -> {scanned, newItems, errors}
get_radar_unread_count() -> i64
clear_radar_cache(before_days?) -> i64
```

### 7.3 搜索 API

```
intent_search(query, conversation_id?) -> SearchResult
get_search_history(limit?) -> Vec<SearchHistoryItem>
clear_search_history() -> void
list_search_sources() -> Vec<SearchSource>
add_search_source(...) -> SearchSource
update_search_source(...) -> void
delete_search_source(id) -> void
refresh_search_cache(source_id?) -> i64
import_search_result(project_url, project_name, category_id?) -> {projectId}
```

## 8. 通过标准

### 7.1 MVP 发布前必须满足

| 标准 | 说明 |
|------|------|
| P0-001 ~ P0-007 | 全部平台导入成功 |
| P0-008 ~ P0-011 | 看板/列表正常显示和操作 |
| P0-012 ~ P0-015 | 项目详情正常显示 |
| P0-016 ~ P0-018 | AI 功能可用 |
| P0-019 ~ P0-020 | 本地下载正常 |
| P0-021 ~ P0-022 | 加密存储正常 |
| 无 P0 级别问题 | 所有 P0 用例通过 |

### 8.1 M13 发布前必须满足

| 标准 | 说明 |
|------|------|
| 插件基础设施 | FeaturePlugin trait 编译通过，PluginManager 正常启停 |
| plugins.db | 独立配置库初始化成功，预设 radar/search 两条记录 |
| **操作域层次** | 插件域/仓库域/业务域三层分离正确 |
| **配置独立性** | 插件配置存储在 plugins.db，与主库解耦 |
| 4 种适配器 | RSS/WebCrawl/Telegram/PlatformPreset 均可 fetch |
| LLM 解析器 | 原始内容正确解析为项目信息 |
| RadarPlugin | 定时扫描正常，新条目推送通知 |
| SearchPlugin | 三层搜索（本地→缓存→联网）正常 |
| 收件箱操作 | import/blacklist/ignore 操作正确更新 status |
| 搜索历史 | 搜索历史持久化，右侧边栏显示 |
| RadarInbox UI | 信息源在右侧，暗色主题，i18n 全覆盖，主题联动 |
| SearchView UI | 搜索历史在右侧，ChatGPT 风格，来源徽标 |
| Sidebar 徽标 | 雷达按钮显示未读徽标 |
| 仓库隔离 | vault 模式数据库跟随仓库切换 |
| i18n 测试 | zh.json 和 en.json 键一致 |
| release 构建 | 构建成功，Previous 目录更新 |

### 8.2 M13 性能要求

| 指标 | 要求 |
|------|------|
| 雷达扫描（单源） | ≤ 10 秒 |
| 搜索响应（本地） | ≤ 100ms |
| 搜索响应（联网） | ≤ 30 秒 |
| 插件启停 | ≤ 500ms |

### 8.3 M13 安全要求

| 要求 | 说明 |
|------|------|
| 插件隔离 | 插件不能直接访问其他插件的数据库 |
| 配置安全 | 插件配置中不存储明文密钥 |
| 仓库隔离 | vault 模式插件数据跟随仓库，不跨仓库泄露 |

### 8.4 MVP 性能要求

| 指标 | 要求 |
|------|------|
| 启动时间 | ≤ 3 秒 |
| 列表查询（1000 条） | ≤ 30ms |
| 项目导入 | ≤ 10 秒（网络正常时） |
| 内存占用 | ≤ 50MB |

### 8.5 MVP 安全要求

| 要求 | 说明 |
|------|------|
| Token 加密存储 | API Key/Token 必须加密 |
| 无日志泄露 | 敏感信息不打印到日志 |
| 数据库权限 | 仅当前用户可读写 |

---

## 8. 测试数据准备

### 8.1 测试用项目

| 平台 | URL | 说明 |
|------|-----|------|
| GitHub | `https://github.com/facebook/react` | 大型开源项目 |
| GitHub | `https://github.com/vuejs/core` | 中型开源项目 |
| GitHub | `https://github.com/torvalds/linux` | 超大型项目 |
| Gitee | `https://gitee.com/dromara/hutool` | Gitee 项目 |
| NPM | `https://npmjs.com/package/lodash` | NPM 包 |
| NPM | `https://npmjs.com/package/vue` | NPM 框架 |
| PyPI | `https://pypi.org/project/django` | PyPI 包 |
| PyPI | `https://pypi.org/project/requests` | PyPI 工具 |

### 8.2 测试用分类

| 分类名称 | 说明 |
|----------|------|
| 前端框架 | 测试一级分类 |
| 前端框架/React 生态 | 测试二级分类 |
| 前端框架/Vue 生态 | 测试二级分类 |
| 后端服务 | 测试一级分类 |
| 数据库 | 测试一级分类 |
| AI / ML | 测试一级分类 |

---

## 9. 测试执行计划

| 阶段 | 时间 | 内容 |
|------|------|------|
| Phase 0 测试 | 开发过程中 | 基础架构测试 |
| 单元测试 | 每日 | Tauri Commands、Services 单元测试 |
| 集成测试 | 每周 | API 集成测试 |
| E2E 测试 | 里程碑结束时 | 完整流程测试 |
| 发版测试 | MVP 发布前 | 验收测试 |

---

## 11. 文档关联

| 文档 | 说明 |
|------|------|
| `docs/测试文档/测试用例文档.md` | 详细测试用例 |
| `docs/测试文档/测试记录文档.md` | 测试执行记录 |
| `docs/API接口设计说明书.md` | API 契约参考 |
| `docs/数据库设计说明书.md` | 数据库测试参考 |
| `docs/详细设计说明书.md` | 功能设计参考 |
| `docs/M13实施计划.md` | M13 实施计划（Task 清单） |
| `docs/插件接入规范.md` | 插件接入规范 |

## 12. M13 测试执行计划

| 阶段 | 时间 | 内容 |
|------|------|------|
| Task 1-4 测试 | 开发过程中 | 插件基础设施测试 |
| Task 5-6 测试 | 开发过程中 | 信息源引擎测试 |
| Task 7-9 测试 | 开发过程中 | 雷达插件测试 |
| Task 10 测试 | 开发过程中 | 搜索插件测试 |
| Task 11-12 测试 | 开发过程中 | 前端 UI 测试 |
| Task 13-14 测试 | 开发过程中 | 集成测试 |
| 最终验收 | M13 完成 | 完整流程 E2E 测试 |

---

## 13. 版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V1.0 | 2026-06-29 | 初始版本，基于 MVP 功能点 |
| V1.1 | 2026-07-11 | 新增 M13 二期测试范围（插件管理、情报雷达、意图搜索） |

---

**文档结束**
