# OS-Compass 测试用例文档

> **版本**：V1.1
> **日期**：2026-07-07
> **适用阶段**：Phase 1-1 MVP 开发、测试
> **目标读者**：开发团队、测试团队

---

## 一、测试前准备

### 1.1 环境要求

| 项目 | 版本要求 | 检查命令 |
|------|----------|----------|
| Windows | 10+ | - |
| Node.js | 18+ | `node --version` |
| pnpm | 8+ | `pnpm --version` |
| Rust | 1.75+ | `rustc --version` |
| Git | 2.30+ | `git --version` |
| VS Code（可选） | 最新 | `code --version` |

### 1.2 测试账号准备

| 账号类型 | 必要性 | 准备方式 |
|----------|--------|----------|
| GitHub Token | 可选 | GitHub Settings → Developer settings → Personal access tokens |
| Gitee Token | 推荐 | Gitee 设置 → 私人令牌 |
| LLM API Key | **必须** | OpenAI / DeepSeek / Ollama |
| 代理 | 可选（国内必须） | HTTP/SOCKS5 代理 |

### 1.3 测试命令

```powershell
# 1. 克隆仓库
git clone <仓库地址> os-compass
cd os-compass

# 2. 安装依赖
pnpm install

# 3. 启动开发服务器
pnpm tauri dev

# 4. 运行测试
pnpm test
```

---

## 二、项目导入测试

### TC-001：GitHub 公开仓库导入

**目的**：验证 GitHub 公开仓库可以成功导入

**前置条件**：无（公开仓库无需 Token）

**测试步骤**：
1. 启动应用
2. 点击"导入项目"
3. 输入 `https://github.com/facebook/react`
4. 点击"导入"

**预期结果**：
- 显示加载状态
- 导入成功，弹出项目卡片
- 显示项目名称、Star 数、语言
- 健康度评分显示

**通过标准**：`health_score` 字段有值

---

### TC-002：GitHub Token 导入私有仓库

**目的**：验证 GitHub Token 配置后可以导入私有仓库

**前置条件**：拥有私有仓库访问权限的 GitHub Token

**测试步骤**：
1. 打开设置 → 插件配置
2. 配置 GitHub Token
3. 导入私有仓库 URL

**预期结果**：
- Token 配置成功
- 私有仓库导入成功

**通过标准**：私有仓库元数据显示

---

### TC-003：Gitee 仓库导入

**目的**：验证 Gitee 仓库可以成功导入

**前置条件**：Gitee Personal Access Token

**测试步骤**：
1. 打开设置 → 插件配置
2. 配置 Gitee Token
3. 导入 `https://gitee.com/dromara/hutool`

**预期结果**：
- Token 配置成功
- Gitee 项目导入成功

**通过标准**：项目显示正常

---

### TC-004：NPM 包导入

**目的**：验证 NPM 包可以成功导入

**前置条件**：无（NPM 无需认证）

**测试步骤**：
1. 导入 `https://npmjs.com/package/lodash`

**预期结果**：
- 显示包名、版本、下载量
- 健康度评分显示

**通过标准**：`downloads` 字段有值

---

### TC-005：PyPI 包导入

**目的**：验证 PyPI 包可以成功导入

**前置条件**：无（PyPI 无需认证）

**测试步骤**：
1. 导入 `https://pypi.org/project/django`

**预期结果**：
- 显示包名、版本、作者
- 健康度评分显示

**通过标准**：`version` 字段有值

---

### TC-006：未知平台回退

**目的**：验证未知平台有合适的回退提示

**前置条件**：无

**测试步骤**：
1. 导入一个未知平台 URL，如 `https://example.com/project`

**预期结果**：
- 提示平台不支持
- 或提示爬虫服务不可用
- 或引导手动录入

**通过标准**：有明确提示，无崩溃

---

### TC-007：重复导入检测

**目的**：验证重复导入有提示

**前置条件**：已导入 `https://github.com/facebook/react`

**测试步骤**：
1. 再次导入 `https://github.com/facebook/react`

**预期结果**：
- 提示"项目已存在"
- 不创建重复记录

**通过标准**：错误码 `DUPLICATE_PROJECT`

---

## 三、项目列表测试

### TC-008：看板视图显示

**目的**：验证看板视图正常显示四列状态

**前置条件**：已有至少 4 个不同状态的项目

**测试步骤**：
1. 查看看板视图

**预期结果**：
- 四列状态：💡待探索、🔬研究中、✅已落地、🗑️弃用
- 每列显示项目数量
- 项目卡片正确显示

**通过标准**：四列状态正确显示

---

### TC-009：看板拖拽

**目的**：验证项目可以在不同状态间拖拽

**前置条件**：有可拖拽的项目

**测试步骤**：
1. 拖动一个"待探索"项目到"研究中"
2. 观察状态变化

**预期结果**：
- 拖拽流畅
- 状态更新成功
- 数据库状态同步更新

**通过标准**：`lifecycle_status` 更新为 `DIVING`

---

### TC-010：列表视图筛选

**目的**：验证列表筛选功能正常

**前置条件**：有多个不同分类的项目

**测试步骤**：
1. 切换到列表视图
2. 选择分类筛选"前端框架"
3. 观察筛选结果

**预期结果**：
- 只显示"前端框架"分类的项目
- 其他分类项目隐藏

**通过标准**：筛选结果正确

---

### TC-011：关键字搜索

**目的**：验证关键字搜索功能

**前置条件**：有可搜索的项目

**测试步骤**：
1. 输入"react"进行搜索

**预期结果**：
- 显示包含"react"的项目
- 支持模糊匹配

**通过标准**：搜索结果包含"react"

---

## 四、项目详情测试

### TC-012：查看概览

**目的**：验证项目概览信息完整

**前置条件**：已导入项目

**测试步骤**：
1. 点击项目卡片进入详情
2. 查看"概览"标签页

**预期结果**：
- 显示项目名称、URL、Star 数
- 显示 AI 一句话总结
- 显示适用场景
- 显示快速操作按钮

**通过标准**：基本信息完整

---

### TC-013：查看 AI 报告

**目的**：验证 AI 报告正常显示

**前置条件**：项目已导入并生成 AI 报告

**测试步骤**：
1. 切换到"AI 报告"标签页

**预期结果**：
- 显示健康度评分（0-100）
- 显示评分维度（活跃度、Issue 闭环率等）
- 显示 License 风险

**通过标准**：`health_score` 显示

---

### TC-014：查看 README

**目的**：验证 README Markdown 渲染正常

**前置条件**：项目 README 已抓取

**测试步骤**：
1. 切换到"README"标签页

**预期结果**：
- Markdown 正常渲染
- 代码块语法高亮
- 图片正常显示

**通过标准**：渲染无异常

---

### TC-015：添加本地笔记

**目的**：验证笔记可以添加和保存

**前置条件**：进入项目详情

**测试步骤**：
1. 切换到"本地笔记"标签页
2. 点击"添加笔记"
3. 输入笔记内容
4. 保存

**预期结果**：
- 笔记保存成功
- 刷新页面后笔记仍存在

**通过标准**：笔记持久化

---

## 五、AI 能力测试

### TC-016：LLM 配置和连接测试

**目的**：验证 LLM 配置正确后可以连接

**前置条件**：有效的 LLM API Key

**测试步骤**：
1. 打开设置 → LLM 配置
2. 填写 API Base URL、API Key、模型名称
3. 点击"测试连接"

**预期结果**：
- 显示"连接成功"
- 无错误提示

**通过标准**：`test_llm_connection` 返回 `true`

---

### TC-017：导入时 AI 分析

**目的**：验证导入时可以生成 AI 分析

**前置条件**：LLM 已配置

**测试步骤**：
1. 导入新项目
2. 勾选"自动生成 AI 报告"

**预期结果**：
- AI 报告生成成功
- 健康度评分显示

**通过标准**：`ai_report` 字段有值

---

### TC-018：README 翻译

**目的**：验证 README 可以切换语言

**前置条件**：项目 README 为非中文

**测试步骤**：
1. 查看 README
2. 选择"中文版"

**预期结果**：
- 显示中文翻译
- 翻译内容可读

**通过标准**：显示翻译版本

---

### TC-018B：翻译引擎 - Google 翻译

**目的**：验证使用 Google 翻译引擎进行翻译

**前置条件**：已配置翻译引擎为 Google，或使用自动选择

**测试步骤**：
1. 进入设置 → 翻译
2. 确认翻译引擎选择为"自动选择"或"Google 翻译"
3. 导入一个英文 README 项目
4. 查看 README 翻译

**预期结果**：
- 短文本（&lt;5000字符）使用 Google 翻译
- 翻译速度快
- 翻译内容正确

**通过标准**：Google 翻译正常工作

---

### TC-018C：翻译引擎 - LLM 翻译

**目的**：验证使用 LLM 翻译引擎进行翻译

**前置条件**：已配置翻译引擎为 LLM，且 LLM 配置正确

**测试步骤**：
1. 进入设置 → 翻译
2. 设置翻译引擎为"LLM 翻译"
3. 导入一个英文 README 项目
4. 查看 README 翻译

**预期结果**：
- 使用 LLM 进行翻译
- 翻译质量较高
- 适合长文本

**通过标准**：LLM 翻译正常工作

---

### TC-018D：翻译引擎 - 自动选择策略

**目的**：验证自动选择策略正确选择翻译引擎

**前置条件**：Google 和 LLM 都已配置

**测试步骤**：
1. 进入设置 → 翻译
2. 设置翻译引擎为"自动选择（推荐）"
3. 导入两个项目：一个短 README，一个长 README（&gt;5000字符）
4. 分别查看翻译

**预期结果**：
- 短 README 使用 Google 翻译
- 长 README 使用 LLM 翻译
- Google 翻译失败时自动降级到 LLM

**通过标准**：自动选择策略生效

---

### TC-018E：Google 翻译代理配置

**目的**：验证 Google 翻译代理配置正确

**前置条件**：中国大陆用户，需要代理访问 Google

**测试步骤**：
1. 进入设置 → 翻译
2. 开启"使用代理访问 Google"
3. 配置代理地址和端口
4. 尝试翻译英文 README

**预期结果**：
- 通过代理成功访问 Google 翻译
- 翻译正常显示

**通过标准**：Google 翻译通过代理正常工作

---

### TC-018F：翻译保存和读取

**目的**：验证翻译结果正确保存和读取

**前置条件**：项目已翻译

**测试步骤**：
1. 翻译一个项目
2. 关闭项目详情
3. 重新打开项目详情
4. 点击翻译

**预期结果**：
- 不需要重新翻译，直接显示已保存的翻译结果
- 可以切换原文/翻译显示

**通过标准**：翻译保存和读取正常工作

---

### TC-018G：导入URL唯一性校验

**目的**：验证重复导入相同URL时提示用户

**前置条件**：数据库中已存在 URL 为 `https://github.com/frappe/crm` 的项目

**测试步骤**：
1. 打开导入对话框
2. 输入已存在的 URL：`https://github.com/frappe/crm`
3. 点击导入

**预期结果**：
- 弹出提示框："该项目已存在（项目名：crm），是否跳转到该项目详情？"
- 提供"查看项目"和"取消"按钮
- 不创建新的项目记录

**通过标准**：数据库中该 URL 仍只有一条记录

---

### TC-018H：URL规范化校验

**目的**：验证URL规范化后能正确识别重复

**前置条件**：数据库中已存在 URL 为 `https://github.com/frappe/crm` 的项目

**测试步骤**：
1. 输入 `https://github.com/frappe/crm/`（带尾部斜杠）
2. 点击导入

**预期结果**：
- 识别为重复项目，弹出提示

**通过标准**：尾部斜杠不影响重复判断

---

### TC-018I：归档项目不参与唯一性校验

**目的**：验证已归档项目不阻止重新导入

**前置条件**：数据库中存在 URL 为 `https://github.com/old/project` 的已归档项目（data_status = 'ARCHIVED'）

**测试步骤**：
1. 输入 `https://github.com/old/project`
2. 点击导入

**预期结果**：
- 允许导入，不提示重复
- 创建新的项目记录

**通过标准**：归档项目可重新导入

---

### TC-018J：异步导入 - 基本信息快速入库

**目的**：验证导入时基本信息快速入库，用户可立即查看

**前置条件**：配置了有效的 GitHub Token

**测试步骤**：
1. 输入 `https://github.com/some/project`
2. 点击导入

**预期结果**：
- 3-5秒内项目出现在看板视图
- 项目状态为 TO_EXPLORE，健康度显示"待评分"
- AI 分析、翻译等模块显示加载状态

**通过标准**：基本信息入库时间 < 5秒

---

### TC-018K：异步导入 - AI分析异步执行

**目的**：验证AI分析在后台异步执行，完成后自动更新

**前置条件**：配置了有效的 LLM，勾选"自动分析"

**测试步骤**：
1. 导入项目（基本信息已入库）
2. 进入项目详情页

**预期结果**：
- AI 一句话总结模块显示"分析中..."spinner
- 适用场景模块显示"分析中..."spinner
- 分析完成后自动显示结果，无需手动刷新

**通过标准**：AI 分析异步完成并自动更新 UI

---

### TC-018L：异步导入 - 翻译异步执行

**目的**：验证翻译在后台异步执行，各模块独立显示进度

**前置条件**：配置了翻译引擎，勾选"自动翻译"

**测试步骤**：
1. 导入英文项目（基本信息已入库）
2. 进入项目详情页

**预期结果**：
- 项目简介模块显示"翻译中..."spinner
- AI 一句话总结模块显示"翻译中..."spinner
- 各模块翻译完成后独立更新

**通过标准**：各模块翻译独立完成并自动更新

---

### TC-018M：异步导入 - 单任务失败不影响其他

**目的**：验证单个异步任务失败时其他任务正常执行

**前置条件**：LLM 配置有效但翻译引擎配置无效

**测试步骤**：
1. 导入项目
2. 等待所有异步任务完成

**预期结果**：
- AI 分析正常完成
- 翻译任务失败，显示错误提示
- 其他任务不受影响

**通过标准**：单任务失败不阻断其他任务

---

### TC-018N：翻译引擎使用范围 - 概览用 LLM

**目的**：验证概览翻译使用 LLM 引擎而非 Google

**前置条件**：配置了有效的 LLM

**测试步骤**：
1. 导入英文项目
2. 等待后台任务完成
3. 进入项目详情，检查 translations 表

**预期结果**：
- summary、use_cases、risks、dependencies、description 都有翻译记录
- 翻译质量为 LLM 级别（语义准确，非机翻）

**通过标准**：5 个概览字段都有 LLM 翻译结果

---

### TC-018O：翻译引擎使用范围 - README 用自动选择

**目的**：验证 README 翻译使用自动选择引擎（Google 优先）

**前置条件**：配置了有效的 LLM，Google 翻译可用

**测试步骤**：
1. 导入英文项目
2. 等待后台任务完成
3. 进入项目详情，打开 README 标签页

**预期结果**：
- README 翻译完成
- 翻译速度较快（Google 翻译）

**通过标准**：README 翻译成功

---

### TC-018P：翻译失败自动补译

**目的**：验证后台任务部分字段翻译失败后，前端自动补译

**前置条件**：模拟某些字段翻译失败（如 LLM 限流）

**测试步骤**：
1. 导入项目
2. 后台任务完成后，检查 translations 表（可能缺少部分字段）
3. 进入项目详情页

**预期结果**：
- 缺失字段显示 spinner（翻译中）
- 自动用 LLM 补译缺失字段
- 补译完成后显示中文内容

**通过标准**：所有有原文的字段最终都有翻译

---

### TC-018Q：手动补译缺失翻译

**目的**：验证点击"翻译概览"按钮补译缺失字段

**前置条件**：项目已有部分翻译，缺少 summary 和 use_cases

**测试步骤**：
1. 进入项目详情
2. 点击"翻译概览"按钮

**预期结果**：
- 已有翻译的字段不重新翻译
- 缺失的字段显示 spinner 并翻译
- 翻译完成后保存到数据库

**通过标准**：点击翻译后所有字段都有翻译

---

### TC-018R：AI 分析 README 预处理

**目的**：验证大 README 项目 AI 分析速度提升

**前置条件**：导入 README 很大的项目（如 comfyui-portrait-master-zh-cn）

**测试步骤**：
1. 导入大 README 项目
2. 等待 AI 分析完成
3. 检查 AI 分析结果质量

**预期结果**：
- AI 分析速度明显提升（相比全量 README）
- AI 分析结果准确（基于截取的前 6000 字符）
- README 模块展示完整内容（不受预处理影响）

**通过标准**：AI 分析在合理时间内完成，结果准确

---

### TC-018S：后台任务生命周期

**目的**：验证关闭导入页面和项目详情不影响后台任务

**前置条件**：批量导入多个项目

**测试步骤**：
1. 批量导入 3 个项目
2. 导入页面自动关闭后，立即打开第一个项目详情
3. 关闭项目详情
4. 等待一段时间后打开第二个项目详情

**预期结果**：
- 所有项目的后台任务都正常完成
- AI 分析、翻译、标签都正确生成
- 关闭页面不影响后台任务执行

**通过标准**：3 个项目都有完整的 AI 分析和翻译

---

### TC-018T：翻译引擎设置持久化

**目的**：验证翻译引擎设置可更改并保存

**前置条件**：打开设置中心 → 翻译标签页

**测试步骤**：
1. 查看翻译引擎默认值
2. 切换为"Google 翻译"
3. 保存设置
4. 重新打开设置中心

**预期结果**：
- 默认显示"自动选择（推荐）"
- 切换后保存成功
- 重新打开仍显示"Google 翻译"

**通过标准**：设置可更改、保存、持久化

---

### TC-018U：Markdown 外链浏览器打开

**目的**：验证 README/RUNBOOK 中的超链接在系统浏览器打开

**前置条件**：项目有包含超链接的 README

**测试步骤**：
1. 打开项目详情 → README 标签页
2. 点击 README 中的超链接

**预期结果**：
- 系统浏览器打开对应 URL
- 程序内不跳转，保持当前页面

**通过标准**：超链接在系统浏览器打开

---

### TC-018V：看板拖拽跨泳道

**目的**：验证项目卡片可在四个泳道间拖拽

**前置条件**：看板视图有项目

**测试步骤**：
1. 按住项目卡片
2. 拖拽到另一个泳道
3. 松开鼠标

**预期结果**：
- 拖拽时显示半透明浮层跟随鼠标
- 目标泳道高亮
- 松开后项目移动到目标泳道
- 项目状态更新

**通过标准**：拖拽后项目状态正确变更

---

### TC-018W：项目详情页状态即时更新

**目的**：验证详情页切换状态后立即显示新状态

**前置条件**：打开项目详情页

**测试步骤**：
1. 点击状态栏的其他状态按钮

**预期结果**：
- 状态按钮立即切换高亮
- 无需关闭重开详情页

**通过标准**：状态切换即时反映

---

## 六、克隆功能测试

### TC-019：克隆标签页记录自动创建

**目的**：验证切换到克隆标签页时自动创建记录

**前置条件**：项目无克隆记录

**测试步骤**：
1. 打开项目详情页
2. 切换到"克隆"标签页

**预期结果**：
- 自动创建 project_clone 记录
- 代理设置使用 Settings 默认值填充

**通过标准**：project_clone 表中有对应记录

---

### TC-020：克隆设置保存

**目的**：验证克隆设置可以正确保存

**前置条件**：进入项目详情 → 克隆标签页

**测试步骤**：
1. 修改克隆目录
2. 配置代理设置
3. 点击"保存"

**预期结果**：
- 保存成功
- 下次打开克隆标签页设置保持

**通过标准**：project_clone 表中字段正确保存

---

### TC-021：克隆命令生成

**目的**：验证克隆命令正确生成（含代理参数）

**前置条件**：已保存克隆设置

**测试步骤**：
1. 进入项目详情 → 克隆标签页
2. 查看克隆命令区域

**预期结果**：
- 命令显示完整 git clone 命令
- 如启用代理，命令包含代理设置

**通过标准**：命令格式正确

---

### TC-022：复制克隆命令

**目的**：验证克隆命令可以复制到剪贴板

**前置条件**：进入项目详情 → 克隆标签页

**测试步骤**：
1. 点击"复制命令"
2. 粘贴到终端

**预期结果**：
- 复制成功提示
- 粘贴内容与显示命令一致

**通过标准**：剪贴板内容正确

---

### TC-023：执行克隆

**目的**：验证程序可以执行 git clone

**前置条件**：
- 已保存克隆设置
- 克隆目录存在且可写
- Git 已安装

**测试步骤**：
1. 点击"执行克隆"
2. 等待克隆完成

**预期结果**：
- 克隆成功提示
- cloned_path 和 cloned_at 更新
- 项目文件存在于目标目录

**通过标准**：克隆完成，字段正确更新

---

### TC-024：克隆目录已存在

**目的**：验证目标目录已存在时的错误处理

**前置条件**：克隆目录中已有同名项目

**测试步骤**：
1. 点击"执行克隆"

**预期结果**：
- 显示错误提示"目录已存在"
- 不覆盖已有目录

**通过标准**：错误处理正确

---

### TC-025：不同项目不同代理

**目的**：验证不同项目可以使用不同的代理配置

**前置条件**：两个项目 A 和 B

**测试步骤**：
1. 项目 A 配置代理 X
2. 项目 B 配置代理 Y
3. 分别保存

**预期结果**：
- 项目 A 使用代理 X
- 项目 B 使用代理 Y
- 各自独立，互不影响

**通过标准**：project_clone 表中两条记录代理配置独立

---

## 七、Releases 标签页测试

### TC-026：Releases 标签页自动加载

**目的**：验证首次进入 Releases 标签页时自动抓取数据

**前置条件**：已导入一个 GitHub 项目

**测试步骤**：
1. 打开项目详情
2. 点击"Releases"标签页

**预期结果**：
- 显示 loading 状态
- 自动抓取 Releases 数据
- 列表按发布时间倒序显示

**通过标准**：Releases 列表正确显示

---

### TC-027：Releases 手动刷新

**目的**：验证手动刷新按钮可以重新抓取数据

**前置条件**：Releases 标签页已有缓存数据

**测试步骤**：
1. 进入项目详情 → Releases 标签页
2. 查看当前数据
3. 点击"刷新"按钮
4. 等待刷新完成

**预期结果**：
- 按钮显示 loading 状态
- 刷新完成后显示新数据

**通过标准**：数据更新成功

---

### TC-028：Releases 下载功能

**目的**：验证 Releases 下载链接可以正常跳转

**前置条件**：Releases 标签页有可下载项

**测试步骤**：
1. 进入项目详情 → Releases 标签页
2. 点击某个 Release 的"下载"按钮
3. 在下拉菜单中选择一个下载项

**预期结果**：
- 下载下拉菜单正常展开
- 点击下载项后浏览器打开下载链接

**通过标准**：浏览器成功打开下载链接

---

### TC-029：Releases 复制链接

**目的**：验证复制链接功能正常

**前置条件**：Releases 标签页有可下载项

**测试步骤**：
1. 进入项目详情 → Releases 标签页
2. 点击某个 Release 的"复制"按钮

**预期结果**：
- 链接复制到剪贴板
- 显示复制成功提示

**通过标准**：剪贴板内容正确

---

### TC-030：Releases 翻译功能

**目的**：验证 Releases 说明可以翻译

**前置条件**：Releases 标签页有数据

**测试步骤**：
1. 进入项目详情 → Releases 标签页
2. 点击"翻译"按钮
3. 等待翻译完成

**预期结果**：
- 按钮显示 loading 状态
- 翻译完成后显示中文说明

**通过标准**：body_zh 字段有值

---

### TC-031：Releases 空状态

**目的**：验证无 Releases 时显示空状态

**前置条件**：选择一个没有 Releases 的项目

**测试步骤**：
1. 进入项目详情 → Releases 标签页

**预期结果**：
- 显示"暂无 Releases 信息"
- 不显示错误

**通过标准**：空状态正确显示

---

## 八、AI 智能分类测试

### TC-032：AI 分类按钮存在

**目的**：验证分类栏存在 AI 分类按钮

**前置条件**：打开项目详情

**测试步骤**：
1. 进入项目详情
2. 查看分类栏

**预期结果**：
- 分类栏显示"AI 分类"按钮

**通过标准**：按钮可见且可点击

---

### TC-033：AI 智能分类执行

**目的**：验证 AI 分类可以正确匹配分类

**前置条件**：
- 系统中已有多个分类
- 项目有 AI 分析数据

**测试步骤**：
1. 进入项目详情
2. 点击分类栏的"AI 分类"按钮
3. 等待分类完成

**预期结果**：
- 按钮显示 loading 状态
- 分类完成后显示新分类
- Toast 提示分类结果

**通过标准**：项目 category_id 更新正确

---

### TC-034：AI 分类 - 无可用分类

**目的**：验证系统无分类时的错误提示

**前置条件**：系统中无任何分类

**测试步骤**：
1. 清空系统分类
2. 进入项目详情
3. 点击"AI 分类"按钮

**预期结果**：
- 显示错误提示"系统中暂无分类"

**通过标准**：错误提示正确

---

### TC-035：AI 分类 - 项目信息不足

**目的**：验证项目信息不足时的错误提示

**前置条件**：导入一个仅有基本信息的项目（无 AI 分析）

**测试步骤**：
1. 进入该项目的详情页
2. 点击"AI 分类"按钮

**预期结果**：
- 显示错误提示"项目信息不足"

**通过标准**：错误提示正确

---

## 九、数据安全测试

### TC-036：敏感信息加密存储

**目的**：验证敏感信息加密存储

**前置条件**：进入项目详情 → 用户信息

**测试步骤**：
1. 添加一个 Token（如 NPM Token）
2. 标记为"敏感"
3. 保存

**预期结果**：
- 保存成功
- 数据库中字段加密

**通过标准**：数据库字段为密文

---

### TC-037：敏感信息解密显示

**目的**：验证解密后可以正常查看

**前置条件**：已保存敏感信息

**测试步骤**：
1. 查看敏感信息

**预期结果**：
- 显示原文（解密后）
- 无明文泄露

**通过标准**：解密显示正常

---

## 十、分类管理测试

### TC-038：创建一级分类

**目的**：验证可以创建一级分类

**测试步骤**：
1. 打开分类管理
2. 点击"新建分类"
3. 填写名称、描述

**预期结果**：
- 分类创建成功
- 显示在分类树中

**通过标准**：分类持久化

---

### TC-039：创建子分类

**目的**：验证可以创建多级分类

**测试步骤**：
1. 选择一个父分类
2. 创建子分类

**预期结果**：
- 子分类创建成功
- 树形结构正确

**通过标准**：`parent_id` 正确

---

### TC-030：删除空分类

**目的**：验证空分类可以删除

**测试步骤**：
1. 选择无项目的分类
2. 点击删除

**预期结果**：
- 删除成功

**通过标准**：分类删除

---

### TC-031：删除有项目分类

**目的**：验证有项目的分类禁止删除

**测试步骤**：
1. 选择有项目的分类
2. 点击删除

**预期结果**：
- 提示"该分类下有项目，无法删除"

**通过标准**：错误码 `HAS_CHILDREN`

---

## 九、网络与容错测试

### TC-032：网络超时处理

**目的**：验证网络超时有友好提示

**前置条件**：模拟网络超时

**测试步骤**：
1. 断开网络或配置无效代理
2. 尝试导入项目

**预期结果**：
- 显示超时提示
- 不崩溃

**通过标准**：错误码 `NETWORK_TIMEOUT`

---

### TC-033：LLM 调用失败处理

**目的**：验证 LLM 调用失败有友好提示

**前置条件**：配置错误的 LLM API

**测试步骤**：
1. 配置错误的 API Key
2. 尝试导入并分析

**预期结果**：
- 显示 LLM 配置问题提示
- 引导检查配置

**通过标准**：错误码 `LLM_API_ERROR`

---

### TC-034：离线浏览

**目的**：验证离线可以浏览已导入项目

**前置条件**：已导入项目

**测试步骤**：
1. 断开网络
2. 浏览已导入项目

**预期结果**：
- 可以正常浏览
- 已导入数据显示

**通过标准**：离线可用

---

## 十、代理设置测试

### TC-035：HTTP 代理配置

**目的**：验证 HTTP 代理配置生效

**前置条件**：有效的 HTTP 代理

**测试步骤**：
1. 打开设置 → 下载设置
2. 配置 HTTP 代理
3. 尝试导入 GitHub 项目

**预期结果**：
- 通过代理访问 GitHub
- 导入成功

**通过标准**：代理请求成功

---

## 十一、M13 插件基础设施测试

### TC-M13-001：FeaturePlugin trait 编译

**目的**：验证 FeaturePlugin trait 定义完整，编译通过

**前置条件**：Rust 环境正常

**测试步骤**：
1. 执行 `cargo check --lib`

**预期结果**：
- 编译成功，无 error

**通过标准**：无编译错误

---

### TC-M13-002：plugins.db 初始化（重构后）

**目的**：验证独立插件配置库正确创建，预设数据存在

**前置条件**：应用首次启动

**测试步骤**：
1. 检查 `${AppData}/.os-compass/plugins.db` 存在
2. 查询 `SELECT * FROM system_plugins`

**预期结果**：
- plugins.db 文件存在
- system_plugins 表存在
- 预设 radar 和 search 两条记录
- enabled = 0

**通过标准**：radar/search 记录存在

---

### TC-M13-003：list_feature_plugins 命令

**目的**：验证插件列表命令返回正确数据

**前置条件**：应用启动

**测试步骤**：
1. 调用 `list_feature_plugins`

**预期结果**：
- 返回 radar 和 search 两个插件信息
- 包含 id/name/plugin_type/enabled/version 字段

**通过标准**：返回 2 个插件

---

### TC-M13-004：set_plugin_enabled 命令

**目的**：验证插件启用/禁用命令正常

**前置条件**：应用启动

**测试步骤**：
1. 调用 `set_plugin_enabled("radar", true)`
2. 调用 `list_feature_plugins`
3. 调用 `set_plugin_enabled("radar", false)`

**预期结果**：
- 启用后 enabled = 1
- 禁用后 enabled = 0

**通过标准**：状态正确切换

---

### TC-M13-005：plugin_get_db_path 命令

**目的**：验证 vault 模式插件路径正确解析

**前置条件**：应用启动，当前仓库存在

**测试步骤**：
1. 调用 `plugin_get_db_path("radar")`

**预期结果**：
- 返回路径包含仓库目录
- 路径包含 `plugin_radar.db`

**通过标准**：路径解析正确

---

### TC-M13-006：plugins.db 独立配置库验证

**目的**：验证插件配置存储在独立数据库，不依赖主库

**前置条件**：应用首次启动

**测试步骤**：
1. 检查 `${AppData}/.os-compass/plugins.db` 文件存在
2. 查询 `system_plugins` 表
3. 确认主库 `os_compass.db` 中无 `feature_plugins` 表（或已废弃）

**预期结果**：
- plugins.db 存在且包含 system_plugins 表
- 预设 radar 和 search 两条记录
- enabled = 0

**通过标准**：plugins.db 独立存在，主库无插件配置表

---

### TC-M13-007：操作域层次验证

**目的**：验证插件域/仓库域/业务域三层分离正确

**前置条件**：应用启动，有当前仓库

**测试步骤**：
1. 调用 `list_feature_plugins()`
2. 调用 `plugin_get_db_path("radar")`
3. 检查 `${AppData}/.os-compass/plugins.db` 路径
4. 检查 `{vault_dir}/plugin_radar.db` 路径

**预期结果**：
| 域 | 存储位置 | 验证点 |
|----|---------|--------|
| 插件域 | `${AppData}/.os-compass/plugins.db` | plugins.db 文件存在 |
| 仓库域 | 当前仓库 os_compass.db | PluginContext.llm_config 来自仓库 |
| 业务域 | `{vault_dir}/plugin_*.db` | 插件 .db 在仓库目录 |

**通过标准**：三层存储位置正确分离

---

### TC-M13-008：仓库切换后插件域不变

**目的**：验证系统级插件配置不受仓库切换影响

**前置条件**：存在多个仓库

**测试步骤**：
1. 在仓库 A 调用 `set_plugin_enabled("radar", true)`
2. 调用 `switch_vault()` 切换到仓库 B
3. 调用 `list_feature_plugins()` 检查 radar enabled 状态
4. 检查 `{vault_dir}/plugin_radar.db`（仓库 B）是否存在

**预期结果**：
| 检查项 | 预期结果 |
|--------|----------|
| radar enabled | 与仓库 A 一致（true） |
| 仓库 B 的 plugin_radar.db | 文件存在但为空（新建） |

**通过标准**：插件域（enabled）跨仓库不变，业务域跟随仓库切换

---

## 十二、M13 信息源引擎测试

### TC-M13-009：RssAdapter 解析 RSS

**目的**：验证 RSS 适配器正确解析 RSS feed

**前置条件**：有可用的 RSS feed URL

**测试步骤**：
1. 添加一个 RSS 信息源（如 GitHub Trending RSS）
2. 触发扫描

**预期结果**：
- 返回 RawContent 列表
- 包含 title/url/content 字段

**通过标准**：RSS 解析成功

---

### TC-M13-007：WebCrawlAdapter 爬取网页

**目的**：验证网页爬取适配器正常

**前置条件**：web2md 爬虫服务运行在 localhost:8080

**测试步骤**：
1. 添加一个 web_crawl 类型信息源
2. 触发扫描

**预期结果**：
- 返回 Markdown 内容
- URL 正确

**通过标准**：网页爬取成功

---

### TC-M13-008：TelegramAdapter 爬取 TG 频道

**目的**：验证 TG 频道爬取适配器正常

**前置条件**：有效的 t.me/s/ 频道 URL

**测试步骤**：
1. 添加一个 telegram 类型信息源
2. 触发扫描

**预期结果**：
- 返回包含 GitHub/Gitee 链接的 RawContent

**通过标准**：TG 频道爬取成功

---

### TC-M13-009：LLM 解析原始内容

**目的**：验证 LLM 解析器将原始内容转为结构化信息

**前置条件**：LLM 配置正确

**测试步骤**：
1. 触发雷达扫描
2. 检查 radar_items 表

**预期结果**：
- project_name/project_url/description 字段有值

**通过标准**：LLM 解析成功

---

## 十三、M13 情报雷达测试

### TC-M13-010：雷达信息源 CRUD

**目的**：验证雷达信息源增删改查正常

**前置条件**：应用启动，雷达插件已启用

**测试步骤**：
1. 调用 `add_radar_source` 添加 RSS 源
2. 调用 `list_radar_sources`
3. 调用 `update_radar_source` 修改名称
4. 调用 `delete_radar_source` 删除

**预期结果**：
- 添加成功返回 RadarSource
- 列表包含新源
- 修改成功
- 删除成功

**通过标准**：CRUD 操作正常

---

### TC-M13-011：trigger_radar_scan 扫描

**目的**：验证手动触发雷达扫描正常

**前置条件**：已添加雷达信息源

**测试步骤**：
1. 调用 `trigger_radar_scan`

**预期结果**：
- 返回 {scanned, newItems, errors}
- scanned > 0
- 扫描完成无报错

**通过标准**：扫描正常执行

---

### TC-M13-012：get_radar_items 收件箱

**目的**：验证收件箱列表正常

**前置条件**：已有雷达扫描记录

**测试步骤**：
1. 调用 `get_radar_items({status: "unread"})`

**预期结果**：
- 返回未读条目列表
- 包含 title/url/description

**通过标准**：收件箱列表正常

---

### TC-M13-013：radar_item_action 导入

**目的**：验证条目导入到主库

**前置条件**：收件箱有未读条目

**测试步骤**：
1. 获取一个未读条目的 item_id
2. 调用 `radar_item_action(item_id, "import")`

**预期结果**：
- 返回 {projectId}
- 收件箱中该条目 status 变为 "imported"

**通过标准**：导入成功，status 更新

---

### TC-M13-014：radar_item_action 黑名单

**目的**：验证条目加入黑名单

**前置条件**：收件箱有未读条目

**测试步骤**：
1. 获取一个未读条目的 item_id
2. 调用 `radar_item_action(item_id, "blacklist")`

**预期结果**：
- 收件箱中该条目 status 变为 "blacklisted"

**通过标准**：黑名单功能正常

---

### TC-M13-015：get_radar_unread_count 徽标

**目的**：验证未读数量正确

**前置条件**：收件箱有未读条目

**测试步骤**：
1. 调用 `get_radar_unread_count`

**预期结果**：
- 返回未读条目数量

**通过标准**：数量正确

---

### TC-M13-016：定时扫描循环

**目的**：验证定时扫描后台任务正常运行

**前置条件**：雷达插件已启用，配置了 check_interval

**测试步骤**：
1. 启用雷达插件
2. 等待一个 check_interval 时间
3. 检查 radar_items 表

**预期结果**：
- 新条目被自动发现

**通过标准**：定时扫描正常

---

## 十四、M13 意图搜索测试

### TC-M13-017：intent_search 本地搜索

**目的**：验证意图搜索本地库优先

**前置条件**：主库有项目数据

**测试步骤**：
1. 调用 `intent_search("react")`

**预期结果**：
- 返回 SearchResult
- results 包含本地库项目
- source = "local"

**通过标准**：本地搜索正常

---

### TC-M13-018：intent_search 三层搜索

**目的**：验证三层搜索按顺序执行

**前置条件**：本地库结果 < 5

**测试步骤**：
1. 调用 `intent_search("机器学习")`
2. 检查返回结果

**预期结果**：
- 先返回本地库结果
- 再返回缓存结果（source = "cache"）
- 最后返回联网结果（source = "web"）

**通过标准**：三层搜索正常

---

### TC-M13-019：搜索历史持久化

**目的**：验证搜索历史正确保存

**前置条件**：执行过搜索

**测试步骤**：
1. 调用 `intent_search("test")`
2. 调用 `get_search_history`

**预期结果**：
- 历史记录包含 "test" 查询
- 按时间倒序

**通过标准**：历史持久化正常

---

### TC-M13-020：clear_search_history

**目的**：验证清空搜索历史功能

**前置条件**：有搜索历史

**测试步骤**：
1. 调用 `clear_search_history`
2. 调用 `get_search_history`

**预期结果**：
- 返回空列表

**通过标准**：清空功能正常

---

### TC-M13-021：import_search_result

**目的**：验证搜索结果导入到主库

**前置条件**：搜索结果有可导入项目

**测试步骤**：
1. 调用 `intent_search("vue")`
2. 调用 `import_search_result(project_url, project_name)`

**预期结果**：
- 返回 {projectId}
- 项目出现在主库

**通过标准**：导入功能正常

---

## 十五、M13 前端 UI 测试

### TC-M13-022：RadarInbox 视图

**目的**：验证情报雷达收件箱 UI 正常

**前置条件**：雷达插件已启用，有扫描记录

**测试步骤**：
1. 点击 Sidebar 雷达按钮
2. 查看 RadarInbox 视图

**预期结果**：
- 右侧显示信息源列表
- 主区域显示条目列表
- 暗色主题正常

**通过标准**：UI 与原型一致

---

### TC-M13-023：SearchView 视图

**目的**：验证意图搜索 UI 正常

**前置条件**：雷达插件已启用

**测试步骤**：
1. 点击 Sidebar 搜索按钮
2. 查看 SearchView 视图

**预期结果**：
- ChatGPT 风格对话界面
- 右侧显示搜索历史
- 暗色主题正常

**通过标准**：UI 与原型一致

---

### TC-M13-024：Sidebar 雷达徽标

**目的**：验证雷达未读徽标显示

**前置条件**：收件箱有未读条目

**测试步骤**：
1. 查看 Sidebar 雷达按钮

**预期结果**：
- 显示红色徽标
- 数字为未读数量

**通过标准**：徽标正确显示

---

### TC-M13-025：插件管理面板

**目的**：验证设置中心插件管理标签页

**前置条件**：应用启动

**测试步骤**：
1. 打开设置
2. 切换到插件管理标签页

**预期结果**：
- 显示插件列表
- 有启用/禁用 toggle

**通过标准**：插件管理面板正常

---

## 十六、M13 仓库隔离测试

### TC-M13-026：vault 模式跟随仓库

**目的**：验证 vault 模式插件数据库跟随仓库切换

**前置条件**：有两个仓库

**测试步骤**：
1. 在仓库 A 添加雷达信息源
2. 切换到仓库 B
3. 调用 `list_radar_sources`

**预期结果**：
- 仓库 B 的 radar_sources 为空
- radar 数据库文件在各自仓库目录

**通过标准**：仓库隔离正常

---

## 十七、测试数据清理

### 17.1 M13 测试数据清理

```bash
# 删除插件数据库文件
# 在仓库目录下删除
rm -rf <vault_dir>/plugin_radar.db
rm -rf <vault_dir>/plugin_search.db

# 或者在应用内通过设置清除
```

### 17.2 M13 测试命令

```powershell
# Rust 编译检查
cd os-compass/src-tauri
$env:PATH="D:\Soft\msys64\ucrt64\bin;D:\Soft\msys64\usr\bin;$env:PATH"
cargo check --lib

# 前端类型检查
cd os-compass
pnpm tsc --noEmit

# 前端测试
pnpm test
```

---

## 十八、测试执行记录

> 测试完成后，请将结果记录在 `docs/测试文档/测试记录文档.md`

---

## 十九、版本记录

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| V1.0 | 2026-06-29 | 初始版本，基于 MVP 功能点 |
| V1.1 | 2026-07-07 | 新增翻译引擎测试用例（TC-018B ~ TC-018W）|
| V1.2 | 2026-07-11 | 新增 M13 测试用例（TC-M13-001 ~ TC-M13-026）|

---

**文档结束**
