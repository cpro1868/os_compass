# M15: 主页增强 + 用户信息 + 雷达增强 实施计划

> **二期功能标记**：本计划所有功能均为二期（Phase 2）功能。

---

## 1 概述

### 1.1 目标

| 模块 | 目标 |
|------|------|
| ~~主页增强~~ | ⬅️ 移至 M16：需求待完善 |
| **仓库路径修复** | 修复默认仓库路径错误（严重BUG） |
| **用户信息** | 实现项目级敏感信息存储（数据保险箱） |
| **雷达搜索增强** | 对已采集数据的关键字/来源/时间筛选 + 分页 |
| **广告过滤插件** | 基于 banlex 的广告和垃圾信息过滤 |

### 1.2 核心价值

> **极简输入（消灭手动填表）**：界面上只有一个大大的输入框。用户只需要粘贴一个链接，敲击回车，操作就结束了。

---

## 2 仓库路径修复（严重BUG）

### 2.1 问题描述

**BUG**：仓库库被错误地放在了系统库目录下，违反设计规范。

### 2.2 系统库与仓库库规范

| 类型 | 获取 API | 作用 |
|------|----------|------|
| **系统库** | `BaseDirs.data_dir().join(".os-compass")` | 全局共享数据（插件、雷达、系统设置） |
| **仓库库** | `app.path().app_data_dir().join("vaults")` | 项目私有数据 |

### 2.3 问题代码 (lib.rs)

仓库相关代码错误使用了 `app.path().app_data_dir()`，导致仓库库被放在了系统库目录下。

### 2.4 修复方案

1. **代码修复**：修改仓库路径获取逻辑
2. **数据迁移**：将现有仓库数据迁移到正确位置

### 2.5 迁移方案

```
源：C:\Users\Administrator\AppData\Roaming\.os-compass\
目标：C:\Users\Administrator\AppData\Local\com.administrator.os-compass\vaults\default\

迁移文件：
├── os_compass.db      ← 仓库数据库
└── .cryptokey         ← 仓库独立密钥（必须迁移，否则数据无法解密）
```

**注意**：`.cryptokey` 必须与数据库一起迁移，否则仓库数据无法解密。

迁移步骤：
1. 检测仓库是否在错误位置（`Roaming\.os-compass\os_compass.db`）
2. 创建目标目录结构
3. 复制数据库文件 `os_compass.db`
4. 复制加密密钥 `.cryptokey`（必须！）
5. 更新 `vault-index.json` 路径
6. 更新 `last-vault.txt` 路径
7. 验证迁移成功

### 2.6 修复文件

- `src-tauri/src/lib.rs` - 路径获取逻辑
- `src-tauri/src/vault.rs` - 仓库迁移逻辑

---

## 3 用户信息模块（数据保险箱）

### 3.1 功能定位

为每个开源项目存储项目相关的私密信息（如 Token、API Key、账户密码等）。

**数据库表**：`project_user_info`（已存在）

```sql
CREATE TABLE project_user_info (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    project_id INTEGER NOT NULL REFERENCES projects(id) ON DELETE CASCADE,
    info_key TEXT NOT NULL,
    info_value TEXT,
    is_secret BOOLEAN DEFAULT FALSE,
    remark TEXT,
    created_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at DATETIME DEFAULT CURRENT_TIMESTAMP,
    UNIQUE(project_id, info_key)
);
```

### 3.2 功能设计

| 功能 | 说明 |
|------|------|
| 列表展示 | 已添加的 key-value 条目 |
| 添加/编辑 | key + value + 敏感开关 + 备注 |
| 删除 | 移除条目 |
| 复制 | 点击复制 value 到剪贴板 |

### 3.3 字段说明

| 字段 | 内容 | 存储 | 展示 | 复制 |
|------|------|------|------|------|
| `info_key` | 用户自定义 | 明文 | 明文 | - |
| `info_value` | 值 | 敏感→加密，明文→明文 | 敏感→脱敏，明文→Markdown | ✅ |
| `remark` | 备注 | 明文 | 明文 | - |

### 3.4 参照

- 原型：`design-system/public/project-detail.html`（用户信息标签页区域）

---

## 4 情报雷达搜索增强

### 4.1 功能定位

对已采集数据进行搜索、筛选、分页展示。

**参照**：`docs/M13实施计划.md` Task 18

### 3.2 功能点

| 功能 | 说明 |
|------|------|
| 关键字搜索 | 模糊匹配 project_name、description |
| 信息源筛选 | 多选下拉 |
| 时间范围筛选 | 开始日期 ~ 结束日期 |
| 分页 | 每页数量可选（10/20/50） |

---

## 5 广告过滤插件

### 5.1 功能定位

情报雷达采集时自动过滤广告和垃圾信息。

**技术方案**：
- **核心库**：banlex（Rust 词库检测库）
- **GitHub**：https://github.com/cognis-digital/banlex
- **混淆检测**：支持 leetspeak、homoglyph、零宽字符
- **内置分类**：spam/scam/harassment
- **中文扩展**：针对 Telegram 和国内平台优化

### 5.2 强度定义

| 强度 | 阈值 | 说明 |
|------|------|------|
| 关闭 | - | 不过滤任何内容 |
| 低 | 2.0 | 只过滤明显广告 |
| 中 | 1.0 | **标准过滤（推荐）** |
| 高 | 0.5 | 激进过滤，可能误杀 |

### 5.3 中文扩展词库

| 分类 | 示例词汇 |
|------|----------|
| 广告类 | 加微信、扫码关注，二维码 |
| 营销类 | 联系我、收徒、培训、课程、优惠券 |
| 行动类 | 立即、点击、限时、福利 |

### 5.4 过滤流程

```
采集内容 → banlex 评分 → 评分 >= 阈值 → 过滤丢弃
                          ↓
                     评分 < 阈值 → 通过检测 → 入库
```

### 5.5 参照

- 原型：`design-system/public/radar-filter-settings.html`

---



## 6 任务分解

| Task | 模块 | 内容 | 工时 | 状态 |
|------|------|------|------|------|
| **Task 0** | 仓库路径 | 修复默认仓库路径错误 | 0.2d | 📋 |
| **Task 1** | 用户信息 | 数据保险箱后端 CRUD | 0.4d | 📋 |
| **Task 2** | 用户信息 | 数据保险箱前端 UI | 0.4d | 📋 |
| **Task 3** | 雷达增强 | 搜索增强后端 API | 0.4d | 📋 |
| **Task 4** | 雷达增强 | 搜索增强前端 UI | 0.5d | 📋 |
| **Task 5** | 广告过滤 | banlex 集成 + 中文词库 | 0.5d | 📋 |
| **Task 6** | 广告过滤 | 过滤设置 UI | 0.3d | 📋 |
| **Task 7** | 广告过滤 | 过滤流程集成 | 0.2d | 📋 |
| ~~Task 8-13~~ | ~~主页~~ | ⬅️ 移至 M16：需求待完善 | - | - |
| **Task 14** | 全局 | i18n + 构建验证 | 0.3d | 📋 |

**总计**：3.2d（约 3d）

---

## 7 实施步骤

### Task 0: 仓库路径修复

**BUG 原因**：代码中仓库相关功能错误使用 `app.path().app_data_dir()` 导致仓库库被放在了系统库目录下。

**影响**：违反设计规范，系统库和仓库库必须分开。

**修复文件**：
- `src-tauri/src/lib.rs` - 路径获取逻辑
- `src-tauri/src/vault.rs` - 仓库迁移逻辑

**迁移方案**：
```
源：C:\Users\Administrator\AppData\Roaming\.os-compass\
目标：C:\Users\Administrator\AppData\Local\com.administrator.os-compass\vaults\default\

迁移文件：
├── os_compass.db      ← 仓库数据库
└── .cryptokey         ← 仓库独立密钥（必须迁移，否则数据无法解密）
```

**迁移步骤**：
1. 检测仓库是否在错误位置（`Roaming\.os-compass\os_compass.db`）
2. 创建目标目录结构
3. 复制数据库文件 `os_compass.db`
4. 复制加密密钥 `.cryptokey`（必须！）
5. 更新 `vault-index.json` 中的路径
6. 更新 `last-vault.txt` 中的路径
7. 验证迁移成功

**验证清单**：
- [ ] 数据库文件在正确位置
- [ ] 密钥文件在正确位置
- [ ] `vault-index.json` 路径已更新
- [ ] `last-vault.txt` 路径已更新
- [ ] 仓库数据可正常读取

---

### Task 1: 数据保险箱后端 CRUD

**Files:**
- Modify: `src-tauri/src/commands/project_cmd.rs`

**接口**：
```rust
#[command]
fn get_project_user_info(project_id: i64) -> Result<Vec<UserInfo>, String>

#[command]
fn add_project_user_info(project_id: i64, key: String, value: String, is_secret: bool, remark: Option<String>) -> Result<i64, String>

#[command]
fn update_project_user_info(id: i64, key: String, value: String, is_secret: bool, remark: Option<String>) -> Result<(), String>

#[command]
fn delete_project_user_info(id: i64) -> Result<(), String>

#[command]
fn copy_user_info_value(id: i64) -> Result<String, String>  // 解密后复制
```

---

### Task 2: 数据保险箱前端 UI

**Files:**
- Modify: `src/components/ProjectDetailDialog.tsx`
- Modify: `src/api/project.ts`

**UI 改动**：
- 替换用户信息标签页占位符为实际组件
- 添加/编辑/删除表单
- 敏感信息脱敏显示
- 复制按钮

---

### Task 3: 搜索增强后端 API

**参照**：`docs/M13实施计划.md` Task 18

**修改**：`get_radar_items` 新增参数
```rust
keyword: Option<String>,
source_ids: Option<Vec<i64>>,
start_date: Option<String>,
end_date: Option<String>,
page: Option<i64>,
page_size: Option<i64>,
```

---

### Task 4: 搜索增强前端 UI

**参照**：`design-system/public/radar-inbox.html`

**UI 改动**：
- 搜索工具栏（关键字/来源/日期）
- 筛选标签
- 分页控件

---

### Task 5: banlex 集成 + 中文词库

**Files:**
- Modify: `src-tauri/Cargo.toml`
- Modify: `src-tauri/src/plugins/radar.rs`

**Step 1: 添加依赖**
```toml
# Cargo.toml
banlex = "0.2"
```

**Step 2: 集成到采集流程**
```rust
use banlex::{Scanner, default_lexicon};

fn should_filter(content: &str, threshold: f64) -> bool {
    let scanner = Scanner::new(default_lexicon(), threshold);
    scanner.scan(content).flagged
}
```

**Step 3: 中文扩展词库**
```rust
// 中文 spam 词库
const ZH_SPAM_WORDS: &[&str] = &[
    "加微信", "扫码关注", "二维码",
    "联系我", "收徒", "培训", "课程",
    "优惠券", "立即", "点击", "限时", "福利",
];
```

---

### Task 6: 过滤设置 UI

**参照**：`design-system/public/radar-filter-settings.html`

**Files:**
- Modify: `src/components/SettingsDialog.tsx`

**UI**：
- 广告过滤开关
- 强度选择（关闭/低/中/高）
- 阈值滑块（高级）

---

### Task 7: 过滤流程集成

**Files:**
- Modify: `src-tauri/src/plugins/radar.rs`

在 LLM 解析后、入库前调用过滤检测。

---

## 8 Self-Review Checklist

### 用户信息模块
- [ ] 数据保险箱后端 CRUD 正常
- [ ] 敏感信息加密存储
- [ ] 复制功能正常
- [ ] 随项目删除级联删除

### 雷达搜索增强
- [ ] 关键字搜索正常工作
- [ ] 信息源筛选正常工作
- [ ] 日期范围筛选正常工作
- [ ] 分页功能正常

### 广告过滤
- [ ] banlex 集成成功
- [ ] 中文词库生效
- [ ] 过滤强度可调
- [ ] 过滤流程集成正确

---

## 9 风险与应对

| 风险 | 影响 | 应对 |
|------|------|------|
| banlex 中文支持 | 过滤不准 | 完善中文词库 |
| 误过滤 | 正常内容被过滤 | 强度可调，默认中档 |
| URL 识别准确性 | 用户体验 | 充分测试各种 URL 格式 |

---

**文档结束**
